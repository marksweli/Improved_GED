# Improved_GED.py 分段逐行详解

> 说明：本文档严格按照 `/home/runner/work/Improved_GED/Improved_GED/Improved_GED.py` 的**主流程运行顺序**来组织内容，但结构上不再是“直接逐行平铺”，而是先**按功能分段给出源码块**，再在每个分段内部**逐行解释**。

> 统一规则：每个分段都先给出带行号的代码，再给出该分段内每一行的解释；涉及计算的部分在解释后附上对应公式，公式统一写成 `[ ... ]` 形式。

> 行数要求：源码共 **487** 行，因此本文档保持远高于 **2435** 行的详细说明规模。

## 主流程分段总览

1. **第 1-12 行：文件头与主题说明阶段** —— 说明脚本主题、作者信息与整体问题背景，为后续的有限元求解提供语义入口。
2. **第 13-19 行：模块导入阶段** —— 导入 FEniCS、mshr、os、sympy、numpy 与 matplotlib，为离散、几何、数组和绘图功能做准备。
3. **第 20-30 行：DOLFIN 全局参数设置阶段** —— 先配置日志级别和 UFL 编译选项，确保后面的大量弱式装配以合适的优化方式执行。
4. **第 31-56 行：非线性求解器参数配置阶段** —— 分别为位移-压力问题和链伸长问题准备 SNES 求解参数，定义收敛阈值、线搜索和线性求解器。
5. **第 57-73 行：局部投影工具函数定义阶段** —— 定义 `local_project`，供后处理时把应力、变形梯度和体积比投影到适合输出的空间。
6. **第 74-92 行：边界与特征点定义阶段** —— 定义底边界、顶边界和中心固定点的几何判据，并实例化为可直接使用的子域对象。
7. **第 93-137 行：材料、损伤、载荷与几何参数初始化阶段** —— 初始化损伤阈值、材料常数、载荷区间、几何尺寸、内尺度以及输出目录命名规则。
8. **第 138-169 行：几何域与网格生成阶段** —— 构造矩形区域、圆环子域和多条水平窄条子域，再生成网格并把网格图保存出来。
9. **第 170-187 行：特征线与特征点输出阶段** —— 把感兴趣的边界线和点标记出来并写入 XDMF，便于可视化检查几何定义是否正确。
10. **第 188-230 行：有限元空间与 J 积分辅助函数构造阶段** —— 建立张量、标量、混合有限元空间，并构造 J 积分权函数 q。
11. **第 231-243 行：位移与伸长边界条件设置阶段** —— 对顶底边界施加位移加载，同时把链伸长变量在上下边界固定为 1。
12. **第 244-290 行：运动学量、应力函数与弹性问题构造阶段** —— 从位移构造 F、C、J、Ic 和链伸长，并定义应力、能量密度、总势能及其残量/Jacobian。
13. **第 291-319 行：伸长场演化问题与约束求解器构造阶段** —— 初始化链伸长历史变量，定义弛豫与粘性项，并建立带上下界的非线性求解器。
14. **第 320-341 行：加载序列与结果容器初始化阶段** —— 创建载荷步数组、能量与迭代容器、XDMF 总输出文件以及若干后处理列表。
15. **第 342-382 行：主加载循环与交替最小化迭代阶段** —— 外层按载荷步推进，内层在固定损伤与固定位移之间交替求解，直到 \Lambda 收敛。
16. **第 383-418 行：状态更新、投影输出与派生量写出阶段** —— 更新历史变量，把主变量和派生量投影并写出，为后处理和可视化准备完整结果集。
17. **第 419-455 行：边界位移更新与后处理阶段** —— 更新下一步边界位移参数，并计算裂纹长度、J 积分和边界反力。
18. **第 456-487 行：结果保存与绘图阶段** —— 把后处理数组保存为文本，再绘制 J-裂纹长度曲线和位移-反力曲线。

## 分段 01：第 1-12 行 —— 文件头与主题说明阶段

- 本段定位：说明脚本主题、作者信息与整体问题背景，为后续的有限元求解提供语义入口。
- 与前后关系：本段结束后会进入“模块导入阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
  1: # FEniCS code Variational Fracture Mechanics
  2: ################################################################################
  3: #
  4: # Modified GED
  5: #
  6: # (For plane-strain cases)
  7: #
  8: # Authors: Mohammad Mousavi
  9: # Email: sm2652@cornell.edu
 10: # date: 06/01/2024
 11: #
 12: ################################################################################
```

### 分段内逐行解释

#### 第 1 行
- 该行代码：`# FEniCS code Variational Fracture Mechanics`
- 逐行解释：文件头第一行直接点明程序主题：它是一个用于变分断裂力学求解的 FEniCS 脚本，后文所有变量和方程都围绕这一目标展开。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“文件头与主题说明阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 2 行
- 该行代码：`################################################################################`
- 逐行解释：这是注释行，作用是给“文件头与主题说明阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“文件头与主题说明阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 3 行
- 该行代码：`#`
- 逐行解释：这是注释行，作用是给“文件头与主题说明阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“文件头与主题说明阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 4 行
- 该行代码：`# Modified GED`
- 逐行解释：这里补充“Modified GED”，说明作者在原始 GED 框架上做了改动，因此阅读时要把它理解为一个具体实现，而不是纯理论推导。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“文件头与主题说明阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 5 行
- 该行代码：`#`
- 逐行解释：这是注释行，作用是给“文件头与主题说明阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“文件头与主题说明阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 6 行
- 该行代码：`# (For plane-strain cases)`
- 逐行解释：这行强调程序针对平面应变情形，意味着本构关系与体积约束的写法都服务于 plane-strain 建模。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“文件头与主题说明阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 7 行
- 该行代码：`#`
- 逐行解释：这是注释行，作用是给“文件头与主题说明阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“文件头与主题说明阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 8 行
- 该行代码：`# Authors: Mohammad Mousavi`
- 逐行解释：这是注释行，作用是给“文件头与主题说明阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“文件头与主题说明阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 9 行
- 该行代码：`# Email: sm2652@cornell.edu`
- 逐行解释：这是注释行，作用是给“文件头与主题说明阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“文件头与主题说明阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 10 行
- 该行代码：`# date: 06/01/2024`
- 逐行解释：这是注释行，作用是给“文件头与主题说明阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“文件头与主题说明阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 11 行
- 该行代码：`#`
- 逐行解释：这是注释行，作用是给“文件头与主题说明阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“文件头与主题说明阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 12 行
- 该行代码：`################################################################################`
- 逐行解释：这是注释行，作用是给“文件头与主题说明阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“文件头与主题说明阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 本段主要是注释、结构组织或 I/O 输出，不以新的连续体力学公式为主。

## 分段 02：第 13-19 行 —— 模块导入阶段

- 本段定位：导入 FEniCS、mshr、os、sympy、numpy 与 matplotlib，为离散、几何、数组和绘图功能做准备。
- 与前后关系：本段结束后会进入“DOLFIN 全局参数设置阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
 13: from __future__ import division
 14: from dolfin import *
 15: from mshr import *
 16: import os
 17: import sympy
 18: import numpy as np
 19: import matplotlib.pyplot as plt
```

### 分段内逐行解释

#### 第 13 行
- 该行代码：`from __future__ import division`
- 逐行解释：这是 `from ... import ...` 形式的导入语句，它把后面有限元离散、网格构造或符号计算需要的名称直接放进当前命名空间。
- 运行影响：这行执行后会在“模块导入阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行是模块导入，不对应独立连续体力学公式。} ]
- 行内顺序关系：这一行属于“模块导入阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 14 行
- 该行代码：`from dolfin import *`
- 逐行解释：这是 `from ... import ...` 形式的导入语句，它把后面有限元离散、网格构造或符号计算需要的名称直接放进当前命名空间。
- 运行影响：这行执行后会在“模块导入阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行是模块导入，不对应独立连续体力学公式。} ]
- 行内顺序关系：这一行属于“模块导入阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 15 行
- 该行代码：`from mshr import *`
- 逐行解释：这是 `from ... import ...` 形式的导入语句，它把后面有限元离散、网格构造或符号计算需要的名称直接放进当前命名空间。
- 运行影响：这行执行后会在“模块导入阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行是模块导入，不对应独立连续体力学公式。} ]
- 行内顺序关系：这一行属于“模块导入阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 16 行
- 该行代码：`import os`
- 逐行解释：这是标准模块导入语句，为后文的文件操作、数组运算或绘图输出提供基础工具。
- 运行影响：这行执行后会在“模块导入阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行是模块导入，不对应独立连续体力学公式。} ]
- 行内顺序关系：这一行属于“模块导入阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 17 行
- 该行代码：`import sympy`
- 逐行解释：这是标准模块导入语句，为后文的文件操作、数组运算或绘图输出提供基础工具。
- 运行影响：这行执行后会在“模块导入阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行是模块导入，不对应独立连续体力学公式。} ]
- 行内顺序关系：这一行属于“模块导入阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 18 行
- 该行代码：`import numpy as np`
- 逐行解释：这是标准模块导入语句，为后文的文件操作、数组运算或绘图输出提供基础工具。
- 运行影响：这行执行后会在“模块导入阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行是模块导入，不对应独立连续体力学公式。} ]
- 行内顺序关系：这一行属于“模块导入阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 19 行
- 该行代码：`import matplotlib.pyplot as plt`
- 逐行解释：这是标准模块导入语句，为后文的文件操作、数组运算或绘图输出提供基础工具。
- 运行影响：这行执行后会在“模块导入阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行是模块导入，不对应独立连续体力学公式。} ]
- 行内顺序关系：这一行属于“模块导入阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 本段主要是注释、结构组织或 I/O 输出，不以新的连续体力学公式为主。

## 分段 03：第 20-30 行 —— DOLFIN 全局参数设置阶段

- 本段定位：先配置日志级别和 UFL 编译选项，确保后面的大量弱式装配以合适的优化方式执行。
- 与前后关系：本段结束后会进入“非线性求解器参数配置阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
 20: 
 21: # Parameters for DOLFIN and SOLVER
 22: # ----------------------------------------------------------------------------
 23: set_log_level(LogLevel.WARNING)  # 20, // information of general interest
 24: 
 25: # set some dolfin specific parameters
 26: parameters["form_compiler"]["representation"]="uflacs"
 27: parameters["form_compiler"]["optimize"]=True
 28: parameters["form_compiler"]["cpp_optimize"]=True
 29: parameters["form_compiler"]["quadrature_degree"]=2
 30: info(parameters,True)
```

### 分段内逐行解释

#### 第 20 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“DOLFIN 全局参数设置阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 21 行
- 该行代码：`# Parameters for DOLFIN and SOLVER`
- 逐行解释：这是注释行，作用是给“DOLFIN 全局参数设置阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“DOLFIN 全局参数设置阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 22 行
- 该行代码：`# ----------------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“DOLFIN 全局参数设置阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“DOLFIN 全局参数设置阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 23 行
- 该行代码：`set_log_level(LogLevel.WARNING)  # 20, // information of general interest`
- 逐行解释：程序启动后先降低 DOLFIN 日志噪声，只保留警告级别信息，避免后续 100 个载荷步的求解日志淹没有效输出。
- 运行影响：这行执行后会在“DOLFIN 全局参数设置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{日志级别} = \mathrm{WARNING} ]
- 行内顺序关系：这一行属于“DOLFIN 全局参数设置阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 24 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“DOLFIN 全局参数设置阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“DOLFIN 全局参数设置阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“DOLFIN 全局参数设置阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 25 行
- 该行代码：`# set some dolfin specific parameters`
- 逐行解释：这是注释行，作用是给“DOLFIN 全局参数设置阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“DOLFIN 全局参数设置阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 26 行
- 该行代码：`parameters["form_compiler"]["representation"]="uflacs"`
- 逐行解释：这一行执行赋值，把右侧表达式 `"uflacs"` 的结果保存到变量 `parameters["form_compiler"]["representation"]` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“DOLFIN 全局参数设置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{representation} = \mathrm{uflacs} ]
- 行内顺序关系：这一行属于“DOLFIN 全局参数设置阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 27 行
- 该行代码：`parameters["form_compiler"]["optimize"]=True`
- 逐行解释：这一行执行赋值，把右侧表达式 `True` 的结果保存到变量 `parameters["form_compiler"]["optimize"]` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“DOLFIN 全局参数设置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{optimize} = \mathrm{True} ]
- 行内顺序关系：这一行属于“DOLFIN 全局参数设置阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 28 行
- 该行代码：`parameters["form_compiler"]["cpp_optimize"]=True`
- 逐行解释：这一行执行赋值，把右侧表达式 `True` 的结果保存到变量 `parameters["form_compiler"]["cpp_optimize"]` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“DOLFIN 全局参数设置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{cpp\_optimize} = \mathrm{True} ]
- 行内顺序关系：这一行属于“DOLFIN 全局参数设置阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 29 行
- 该行代码：`parameters["form_compiler"]["quadrature_degree"]=2`
- 逐行解释：这一行执行赋值，把右侧表达式 `2` 的结果保存到变量 `parameters["form_compiler"]["quadrature_degree"]` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“DOLFIN 全局参数设置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ q_{\mathrm{quad}} = 2 ]
- 行内顺序关系：这一行属于“DOLFIN 全局参数设置阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 30 行
- 该行代码：`info(parameters,True)`
- 逐行解释：把当前 FEniCS 参数打印出来，便于复现实验；这也是排查编译器和积分设置是否生效的直接依据。
- 运行影响：这行执行后会在“DOLFIN 全局参数设置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“DOLFIN 全局参数设置阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 23 行对应公式：[ \text{日志级别} = \mathrm{WARNING} ]
- 第 26 行对应公式：[ \text{representation} = \mathrm{uflacs} ]
- 第 27 行对应公式：[ \text{optimize} = \mathrm{True} ]
- 第 28 行对应公式：[ \text{cpp\_optimize} = \mathrm{True} ]
- 第 29 行对应公式：[ q_{\mathrm{quad}} = 2 ]

## 分段 04：第 31-56 行 —— 非线性求解器参数配置阶段

- 本段定位：分别为位移-压力问题和链伸长问题准备 SNES 求解参数，定义收敛阈值、线搜索和线性求解器。
- 与前后关系：本段结束后会进入“局部投影工具函数定义阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
 31: 
 32: # -----------------------------------------------------------------------------
 33: # parameters of the solvers
 34: solver_u_parameters   = {"nonlinear_solver": "snes",
 35:                          "symmetric": True,
 36:                          "snes_solver": {"linear_solver": "mumps", # lu mumps
 37:                                          "method" : "newtontr",
 38:                                          "line_search": "cp",
 39:                                          "preconditioner" : "hypre_amg",
 40:                                          "maximum_iterations": 300,
 41:                                          "absolute_tolerance": 1e-6,
 42:                                          "relative_tolerance": 1e-6,
 43:                                          "solution_tolerance": 1e-6,
 44:                                          "report": True,
 45:                                          "error_on_nonconvergence": False}}
 46: 
 47: 
 48: solver_Lmbda_parameters = {"nonlinear_solver": "snes",
 49:                           "symmetric": True,
 50:                           "snes_solver": {"maximum_iterations": 300,
 51:                                           "report": True,
 52:                                           "linear_solver": "mumps",
 53:                                           "method": "vinewtonssls",  # if we want to use bounded solver, this should be "vinewtonssls" instead of "newtontr"
 54:                                           "absolute_tolerance": 1e-6,
 55:                                           "relative_tolerance": 1e-6,
 56:                                           "error_on_nonconvergence": False}}
```

### 分段内逐行解释

#### 第 31 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 32 行
- 该行代码：`# -----------------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“非线性求解器参数配置阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 33 行
- 该行代码：`# parameters of the solvers`
- 逐行解释：这是注释行，作用是给“非线性求解器参数配置阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 34 行
- 该行代码：`solver_u_parameters   = {"nonlinear_solver": "snes",`
- 逐行解释：这一行在 FEniCS 中实例化问题对象或求解器对象，把前面定义的弱式、边界条件和 Jacobian 绑定起来。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ solver_u_parameters = {"nonlinear_solver": "snes", ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 35 行
- 该行代码：`                         "symmetric": True,`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 36 行
- 该行代码：`                         "snes_solver": {"linear_solver": "mumps", # lu mumps`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 37 行
- 该行代码：`                                         "method" : "newtontr",`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 38 行
- 该行代码：`                                         "line_search": "cp",`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 39 行
- 该行代码：`                                         "preconditioner" : "hypre_amg",`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 40 行
- 该行代码：`                                         "maximum_iterations": 300,`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 41 行
- 该行代码：`                                         "absolute_tolerance": 1e-6,`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 42 行
- 该行代码：`                                         "relative_tolerance": 1e-6,`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 43 行
- 该行代码：`                                         "solution_tolerance": 1e-6,`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 44 行
- 该行代码：`                                         "report": True,`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 45 行
- 该行代码：`                                         "error_on_nonconvergence": False}}`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 46 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“非线性求解器参数配置阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“非线性求解器参数配置阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 47 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“非线性求解器参数配置阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“非线性求解器参数配置阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 48 行
- 该行代码：`solver_Lmbda_parameters = {"nonlinear_solver": "snes",`
- 逐行解释：这一行在 FEniCS 中实例化问题对象或求解器对象，把前面定义的弱式、边界条件和 Jacobian 绑定起来。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ solver_Lmbda_parameters = {"nonlinear_solver": "snes", ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

#### 第 49 行
- 该行代码：`                          "symmetric": True,`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 19 个执行点，解释顺序与源码顺序保持一致。

#### 第 50 行
- 该行代码：`                          "snes_solver": {"maximum_iterations": 300,`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 20 个执行点，解释顺序与源码顺序保持一致。

#### 第 51 行
- 该行代码：`                                          "report": True,`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 21 个执行点，解释顺序与源码顺序保持一致。

#### 第 52 行
- 该行代码：`                                          "linear_solver": "mumps",`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 22 个执行点，解释顺序与源码顺序保持一致。

#### 第 53 行
- 该行代码：`                                          "method": "vinewtonssls",  # if we want to use bounded solver, this should be "vinewtonssls" instead of "newtontr"`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 23 个执行点，解释顺序与源码顺序保持一致。

#### 第 54 行
- 该行代码：`                                          "absolute_tolerance": 1e-6,`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 24 个执行点，解释顺序与源码顺序保持一致。

#### 第 55 行
- 该行代码：`                                          "relative_tolerance": 1e-6,`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 25 个执行点，解释顺序与源码顺序保持一致。

#### 第 56 行
- 该行代码：`                                          "error_on_nonconvergence": False}}`
- 逐行解释：这一行位于“非线性求解器参数配置阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“非线性求解器参数配置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“非线性求解器参数配置阶段”内部的第 26 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 34 行对应公式：[ solver_u_parameters = {"nonlinear_solver": "snes", ]
- 第 48 行对应公式：[ solver_Lmbda_parameters = {"nonlinear_solver": "snes", ]

## 分段 05：第 57-73 行 —— 局部投影工具函数定义阶段

- 本段定位：定义 `local_project`，供后处理时把应力、变形梯度和体积比投影到适合输出的空间。
- 与前后关系：本段结束后会进入“边界与特征点定义阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
 57: 
 58: 
 59: # Element-wise projection using LocalSolver
 60: def local_project(v, V, u=None):
 61:     dv = TrialFunction(V)
 62:     v_ = TestFunction(V)
 63:     a_proj = inner(dv, v_)*dx
 64:     b_proj = inner(v, v_)*dx
 65:     solver = LocalSolver(a_proj, b_proj)
 66:     solver.factorize()
 67:     if u is None:
 68:         u = Function(V)
 69:         solver.solve_local_rhs(u)
 70:         return u
 71:     else:
 72:         solver.solve_local_rhs(u)
 73:         return
```

### 分段内逐行解释

#### 第 57 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 58 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 59 行
- 该行代码：`# Element-wise projection using LocalSolver`
- 逐行解释：从这一行开始定义一个局部投影工具，它会在后处理阶段把应力、变形梯度和体积比投影到更适合输出的有限元空间。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 60 行
- 该行代码：`def local_project(v, V, u=None):`
- 逐行解释：这一行定义函数 `local_project`，它在“局部投影工具函数定义阶段”中把一段会被重复调用的数学或工具逻辑封装起来，真正执行发生在后续调用时。
- 运行影响：这一行会生成一个离散投影结果，主要服务于输出、后处理或逐点读取。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 61 行
- 该行代码：`    dv = TrialFunction(V)`
- 逐行解释：这一行执行赋值，把右侧表达式 `TrialFunction(V)` 的结果保存到变量 `dv` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ dv = TrialFunction(V) ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 62 行
- 该行代码：`    v_ = TestFunction(V)`
- 逐行解释：这一行执行赋值，把右侧表达式 `TestFunction(V)` 的结果保存到变量 `v_` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ v_ = TestFunction(V) ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 63 行
- 该行代码：`    a_proj = inner(dv, v_)*dx`
- 逐行解释：这一行执行赋值，把右侧表达式 `inner(dv, v_)*dx` 的结果保存到变量 `a_proj` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ a_{\mathrm{proj}}(d\mathbf{v},\mathbf{v}_*) = \int_{\Omega} d\mathbf{v}\cdot\mathbf{v}_*\,\mathrm{d}x ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 64 行
- 该行代码：`    b_proj = inner(v, v_)*dx`
- 逐行解释：这一行执行赋值，把右侧表达式 `inner(v, v_)*dx` 的结果保存到变量 `b_proj` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ b_{\mathrm{proj}}(\mathbf{v},\mathbf{v}_*) = \int_{\Omega} \mathbf{v}\cdot\mathbf{v}_*\,\mathrm{d}x ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 65 行
- 该行代码：`    solver = LocalSolver(a_proj, b_proj)`
- 逐行解释：LocalSolver 使用单元局部矩阵求解投影问题，速度比全局投影更快，适合反复输出张量场。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ solver = LocalSolver(a_proj, b_proj) ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 66 行
- 该行代码：`    solver.factorize()`
- 逐行解释：这一行位于“局部投影工具函数定义阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 67 行
- 该行代码：`    if u is None:`
- 逐行解释：这一行开始一个条件分支，表示程序会根据当前变量状态选择不同路径；这通常意味着模型里存在阈值、边界保护或输出控制。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行体现条件逻辑，而非新的数学算子定义。} ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 68 行
- 该行代码：`        u = Function(V)`
- 逐行解释：这一行执行赋值，把右侧表达式 `Function(V)` 的结果保存到变量 `u` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ u = Function(V) ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 69 行
- 该行代码：`        solver.solve_local_rhs(u)`
- 逐行解释：这一行位于“局部投影工具函数定义阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 70 行
- 该行代码：`        return u`
- 逐行解释：这一行把当前函数内部已经构造好的结果返回给调用方，使上层流程能够直接拿到局部计算结果。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{返回上一步已经构造好的结果，不新增公式。} ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 71 行
- 该行代码：`    else:`
- 逐行解释：这一行表示前述条件均不满足时的兜底路径，用来保证流程完整。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行体现条件逻辑，而非新的数学算子定义。} ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 72 行
- 该行代码：`        solver.solve_local_rhs(u)`
- 逐行解释：这一行位于“局部投影工具函数定义阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 73 行
- 该行代码：`        return`
- 逐行解释：这一行位于“局部投影工具函数定义阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“局部投影工具函数定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“局部投影工具函数定义阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 61 行对应公式：[ dv = TrialFunction(V) ]
- 第 62 行对应公式：[ v_ = TestFunction(V) ]
- 第 63 行对应公式：[ a_{\mathrm{proj}}(d\mathbf{v},\mathbf{v}_*) = \int_{\Omega} d\mathbf{v}\cdot\mathbf{v}_*\,\mathrm{d}x ]
- 第 64 行对应公式：[ b_{\mathrm{proj}}(\mathbf{v},\mathbf{v}_*) = \int_{\Omega} \mathbf{v}\cdot\mathbf{v}_*\,\mathrm{d}x ]
- 第 65 行对应公式：[ solver = LocalSolver(a_proj, b_proj) ]
- 第 68 行对应公式：[ u = Function(V) ]

## 分段 06：第 74-92 行 —— 边界与特征点定义阶段

- 本段定位：定义底边界、顶边界和中心固定点的几何判据，并实例化为可直接使用的子域对象。
- 与前后关系：本段结束后会进入“材料、损伤、载荷与几何参数初始化阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
 74: 
 75: # Define boundary sets for boundary conditions
 76: # ----------------------------------------------------------------------------
 77: class bot_boundary(SubDomain):
 78:     def inside(self, x, on_boundary):
 79:         return on_boundary and near(x[1], 0, 0.01*hsize) # near functuin takes three values: 1. given value 2. target value 3. tolerance
 80: 
 81: class top_boundary(SubDomain):
 82:     def inside(self, x, on_boundary):
 83:         return on_boundary and near(x[1], H, 0.01*hsize)
 84: 
 85: class pin_point(SubDomain):
 86:     def inside(self, x, on_boundary):
 87:         return near(x[0], L/2, hsize) and near(x[1], 0, 0.01*hsize)
 88: 
 89: # Convert all boundary classes for visualization
 90: bot_boundary = bot_boundary()
 91: top_boundary = top_boundary()
 92: pin_point = pin_point()
```

### 分段内逐行解释

#### 第 74 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 75 行
- 该行代码：`# Define boundary sets for boundary conditions`
- 逐行解释：这是注释行，作用是给“边界与特征点定义阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 76 行
- 该行代码：`# ----------------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“边界与特征点定义阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 77 行
- 该行代码：`class bot_boundary(SubDomain):`
- 逐行解释：底边界类在运行早期就定义好，后面既会用于位移边界条件，也会用于给拉伸变量 \Lambda 施加边界约束。
- 运行影响：这行执行后会在“边界与特征点定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 78 行
- 该行代码：`    def inside(self, x, on_boundary):`
- 逐行解释：这一行定义函数 `inside`，它在“边界与特征点定义阶段”中把一段会被重复调用的数学或工具逻辑封装起来，真正执行发生在后续调用时。
- 运行影响：这行执行后会在“边界与特征点定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 79 行
- 该行代码：`        return on_boundary and near(x[1], 0, 0.01*hsize) # near functuin takes three values: 1. given value 2. target value 3. tolerance`
- 逐行解释：这一行把当前函数内部已经构造好的结果返回给调用方，使上层流程能够直接拿到局部计算结果。
- 运行影响：这行执行后会在“边界与特征点定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Gamma_{\mathrm{bot}} = \{ (x,y)\in\partial\Omega \mid |y-0| \le 0.01\,h_{\mathrm{size}} \} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 80 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“边界与特征点定义阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“边界与特征点定义阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 81 行
- 该行代码：`class top_boundary(SubDomain):`
- 逐行解释：顶边界类与底边界相对应，它和底边界共同形成反对称拉伸加载。
- 运行影响：这行执行后会在“边界与特征点定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 82 行
- 该行代码：`    def inside(self, x, on_boundary):`
- 逐行解释：这一行定义函数 `inside`，它在“边界与特征点定义阶段”中把一段会被重复调用的数学或工具逻辑封装起来，真正执行发生在后续调用时。
- 运行影响：这行执行后会在“边界与特征点定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 83 行
- 该行代码：`        return on_boundary and near(x[1], H, 0.01*hsize)`
- 逐行解释：这一行把当前函数内部已经构造好的结果返回给调用方，使上层流程能够直接拿到局部计算结果。
- 运行影响：这行执行后会在“边界与特征点定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Gamma_{\mathrm{top}} = \{ (x,y)\in\partial\Omega \mid |y-H| \le 0.01\,h_{\mathrm{size}} \} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 84 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“边界与特征点定义阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“边界与特征点定义阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 85 行
- 该行代码：`class pin_point(SubDomain):`
- 逐行解释：pin_point 用于标记一个几何特征点，便于后处理或可视化时定位裂纹附近的参考位置。
- 运行影响：这行执行后会在“边界与特征点定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 86 行
- 该行代码：`    def inside(self, x, on_boundary):`
- 逐行解释：这一行定义函数 `inside`，它在“边界与特征点定义阶段”中把一段会被重复调用的数学或工具逻辑封装起来，真正执行发生在后续调用时。
- 运行影响：这行执行后会在“边界与特征点定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 87 行
- 该行代码：`        return near(x[0], L/2, hsize) and near(x[1], 0, 0.01*hsize)`
- 逐行解释：这一行把当前函数内部已经构造好的结果返回给调用方，使上层流程能够直接拿到局部计算结果。
- 运行影响：这行执行后会在“边界与特征点定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Gamma_{\mathrm{pin}} = \{ (x,y) \mid |x-L/2| \le h_{\mathrm{size}},\ |y| \le 0.01\,h_{\mathrm{size}} \} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 88 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“边界与特征点定义阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“边界与特征点定义阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 89 行
- 该行代码：`# Convert all boundary classes for visualization`
- 逐行解释：这是注释行，作用是给“边界与特征点定义阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 90 行
- 该行代码：`bot_boundary = bot_boundary()`
- 逐行解释：类定义结束后立即实例化，这样后面 `mark` 和 `DirichletBC` 可以直接接受对象，而不是类名。
- 运行影响：这行执行后会在“边界与特征点定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ bot_boundary = bot_boundary() ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 91 行
- 该行代码：`top_boundary = top_boundary()`
- 逐行解释：这一行执行赋值，把右侧表达式 `top_boundary()` 的结果保存到变量 `top_boundary` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“边界与特征点定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ top_boundary = top_boundary() ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

#### 第 92 行
- 该行代码：`pin_point = pin_point()`
- 逐行解释：这一行执行赋值，把右侧表达式 `pin_point()` 的结果保存到变量 `pin_point` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“边界与特征点定义阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ pin_point = pin_point() ]
- 行内顺序关系：这一行属于“边界与特征点定义阶段”内部的第 19 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 79 行对应公式：[ \Gamma_{\mathrm{bot}} = \{ (x,y)\in\partial\Omega \mid |y-0| \le 0.01\,h_{\mathrm{size}} \} ]
- 第 83 行对应公式：[ \Gamma_{\mathrm{top}} = \{ (x,y)\in\partial\Omega \mid |y-H| \le 0.01\,h_{\mathrm{size}} \} ]
- 第 87 行对应公式：[ \Gamma_{\mathrm{pin}} = \{ (x,y) \mid |x-L/2| \le h_{\mathrm{size}},\ |y| \le 0.01\,h_{\mathrm{size}} \} ]
- 第 90 行对应公式：[ bot_boundary = bot_boundary() ]
- 第 91 行对应公式：[ top_boundary = top_boundary() ]
- 第 92 行对应公式：[ pin_point = pin_point() ]

## 分段 07：第 93-137 行 —— 材料、损伤、载荷与几何参数初始化阶段

- 本段定位：初始化损伤阈值、材料常数、载荷区间、几何尺寸、内尺度以及输出目录命名规则。
- 与前后关系：本段结束后会进入“几何域与网格生成阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
 93: 
 94: # Damage function
 95: lambda_i = 1.2          # damage threshold
 96: Lmbda_ch_max = 2.7
 97: alpha_damage = 1             # damage coeff: max damage
 98: beta_damage = 20                # damage coeff: determine the sharpness of the damage evolution
 99: 
100: # Material parameters
101: mu    = 1                       # Shear Modulus
102: kappa = 1000                    # Bulk Modulus
103: k_ell = 1e-5                    # Residual stiffness
104: eta = 0                         # Viscosity coeff
105: 
106: # Forces
107: body_force = Constant((0, 0))
108: load_min = 0
109: load_max = 0.6
110: load_steps = 100
111: 
112: # Geometry paramaters
113: L, H = 1, 1                    # Length (x) and height (y-direction)
114: resolution = 40
115: segment = 20
116: hsize = 0.02
117: ell_multi = 2
118: ell = Constant(ell_multi*hsize) # Length parameter
119: 
120: # Numerical parameters of the alternate minimization
121: maxiteration = 100
122: AM_tolerance = 2e-3
123: 
124: # Radius of the outer (2) and inner (1) circles for calculating J integral
125: r2 = 0.47
126: r1 = 0.45
127: cirle_center = 0.5 # for calculating J integral
128: 
129: crack_thickness = 0.04
130: crack_tip = 0.2
131: 
132: g_power = 0.28
133: 
134: # Naming parameters for saving output
135: simulation_params = "eta_%0.1f_ell_%0.2f_Lmbda_ch_max_%0.2f_g_%0.2f_lb_1.5" % (eta, ell, Lmbda_ch_max, g_power)
136: savedir   = simulation_params + "/"
137: 
```

### 分段内逐行解释

#### 第 93 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 94 行
- 该行代码：`# Damage function`
- 逐行解释：这是注释行，作用是给“材料、损伤、载荷与几何参数初始化阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 95 行
- 该行代码：`lambda_i = 1.2          # damage threshold`
- 逐行解释：损伤阈值 \lambda_i 是损伤开始演化的起点；当局部伸长未超过它时，材料保持无损伤状态。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \lambda_i = 1.2 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 96 行
- 该行代码：`Lmbda_ch_max = 2.7`
- 逐行解释：这一行执行赋值，把右侧表达式 `2.7` 的结果保存到变量 `Lmbda_ch_max` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \lambda_{\mathrm{ch}}^{\max} = 2.7 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 97 行
- 该行代码：`alpha_damage = 1             # damage coeff: max damage`
- 逐行解释：这一行执行赋值，把右侧表达式 `1             # damage coeff: max damage` 的结果保存到变量 `alpha_damage` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \alpha_{\mathrm{damage}} = 1 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 98 行
- 该行代码：`beta_damage = 20                # damage coeff: determine the sharpness of the damage evolution`
- 逐行解释：这一行执行赋值，把右侧表达式 `20                # damage coeff: determine the sharpness of the damage evolution` 的结果保存到变量 `beta_damage` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \beta_{\mathrm{damage}} = 20 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 99 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“材料、损伤、载荷与几何参数初始化阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“材料、损伤、载荷与几何参数初始化阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 100 行
- 该行代码：`# Material parameters`
- 逐行解释：这是注释行，作用是给“材料、损伤、载荷与几何参数初始化阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 101 行
- 该行代码：`mu    = 1                       # Shear Modulus`
- 逐行解释：剪切模量 \mu 决定偏应变能的权重，是橡胶类不可压缩材料模型中的核心参数之一。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mu = 1 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 102 行
- 该行代码：`kappa = 1000                    # Bulk Modulus`
- 逐行解释：体积模量 \kappa 取很大值，目的是强烈惩罚体积变化，从数值上逼近不可压缩约束。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \kappa = 1000 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 103 行
- 该行代码：`k_ell = 1e-5                    # Residual stiffness`
- 逐行解释：残余刚度 `k_ell` 防止损伤趋近 1 时刚度完全退化到零，从而避免切线矩阵奇异。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ k_{\ell} = 10^{-5} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 104 行
- 该行代码：`eta = 0                         # Viscosity coeff`
- 逐行解释：这一行执行赋值，把右侧表达式 `0                         # Viscosity coeff` 的结果保存到变量 `eta` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \eta = 0 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 105 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“材料、损伤、载荷与几何参数初始化阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“材料、损伤、载荷与几何参数初始化阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 106 行
- 该行代码：`# Forces`
- 逐行解释：这是注释行，作用是给“材料、损伤、载荷与几何参数初始化阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 107 行
- 该行代码：`body_force = Constant((0, 0))`
- 逐行解释：体力设为零，说明本算例只研究边界位移加载，不考虑重力或体载荷。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathbf{b} = (0,0) ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 108 行
- 该行代码：`load_min = 0`
- 逐行解释：这一行执行赋值，把右侧表达式 `0` 的结果保存到变量 `load_min` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ t_{\min} = 0 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 109 行
- 该行代码：`load_max = 0.6`
- 逐行解释：这一行执行赋值，把右侧表达式 `0.6` 的结果保存到变量 `load_max` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ t_{\max} = 0.6 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 110 行
- 该行代码：`load_steps = 100`
- 逐行解释：这一行执行赋值，把右侧表达式 `100` 的结果保存到变量 `load_steps` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ N_{\mathrm{load}} = 100 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

#### 第 111 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“材料、损伤、载荷与几何参数初始化阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“材料、损伤、载荷与几何参数初始化阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 19 个执行点，解释顺序与源码顺序保持一致。

#### 第 112 行
- 该行代码：`# Geometry paramaters`
- 逐行解释：这是注释行，作用是给“材料、损伤、载荷与几何参数初始化阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 20 个执行点，解释顺序与源码顺序保持一致。

#### 第 113 行
- 该行代码：`L, H = 1, 1                    # Length (x) and height (y-direction)`
- 逐行解释：这里把计算域设置为单位正方形，后续裂纹长度、J 积分半径和位移边界都以它为参考。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ L = 1,\quad H = 1 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 21 个执行点，解释顺序与源码顺序保持一致。

#### 第 114 行
- 该行代码：`resolution = 40`
- 逐行解释：这一行执行赋值，把右侧表达式 `40` 的结果保存到变量 `resolution` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ n_{\mathrm{mesh}} = 40 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 22 个执行点，解释顺序与源码顺序保持一致。

#### 第 115 行
- 该行代码：`segment = 20`
- 逐行解释：这一行执行赋值，把右侧表达式 `20` 的结果保存到变量 `segment` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ segment = 20 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 23 个执行点，解释顺序与源码顺序保持一致。

#### 第 116 行
- 该行代码：`hsize = 0.02`
- 逐行解释：这一行执行赋值，把右侧表达式 `0.02` 的结果保存到变量 `hsize` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ h_{\mathrm{size}} = 0.02 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 24 个执行点，解释顺序与源码顺序保持一致。

#### 第 117 行
- 该行代码：`ell_multi = 2`
- 逐行解释：这一行执行赋值，把右侧表达式 `2` 的结果保存到变量 `ell_multi` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ ell_multi = 2 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 25 个执行点，解释顺序与源码顺序保持一致。

#### 第 118 行
- 该行代码：`ell = Constant(ell_multi*hsize) # Length parameter`
- 逐行解释：内尺度参数 \ell 与网格尺寸绑定，体现相场/梯度损伤模型中“损伤带宽度”与离散尺度的联系。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \ell = 2\,h_{\mathrm{size}} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 26 个执行点，解释顺序与源码顺序保持一致。

#### 第 119 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“材料、损伤、载荷与几何参数初始化阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“材料、损伤、载荷与几何参数初始化阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 27 个执行点，解释顺序与源码顺序保持一致。

#### 第 120 行
- 该行代码：`# Numerical parameters of the alternate minimization`
- 逐行解释：这是注释行，作用是给“材料、损伤、载荷与几何参数初始化阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 28 个执行点，解释顺序与源码顺序保持一致。

#### 第 121 行
- 该行代码：`maxiteration = 100`
- 逐行解释：这一行执行赋值，把右侧表达式 `100` 的结果保存到变量 `maxiteration` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ N_{\mathrm{AM}}^{\max} = 100 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 29 个执行点，解释顺序与源码顺序保持一致。

#### 第 122 行
- 该行代码：`AM_tolerance = 2e-3`
- 逐行解释：这一行执行赋值，把右侧表达式 `2e-3` 的结果保存到变量 `AM_tolerance` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \varepsilon_{\mathrm{AM}} = 2\times 10^{-3} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 30 个执行点，解释顺序与源码顺序保持一致。

#### 第 123 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“材料、损伤、载荷与几何参数初始化阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“材料、损伤、载荷与几何参数初始化阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 31 个执行点，解释顺序与源码顺序保持一致。

#### 第 124 行
- 该行代码：`# Radius of the outer (2) and inner (1) circles for calculating J integral`
- 逐行解释：这是注释行，作用是给“材料、损伤、载荷与几何参数初始化阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 32 个执行点，解释顺序与源码顺序保持一致。

#### 第 125 行
- 该行代码：`r2 = 0.47`
- 逐行解释：这一行执行赋值，把右侧表达式 `0.47` 的结果保存到变量 `r2` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ r_2 = 0.47 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 33 个执行点，解释顺序与源码顺序保持一致。

#### 第 126 行
- 该行代码：`r1 = 0.45`
- 逐行解释：这一行执行赋值，把右侧表达式 `0.45` 的结果保存到变量 `r1` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ r_1 = 0.45 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 34 个执行点，解释顺序与源码顺序保持一致。

#### 第 127 行
- 该行代码：`cirle_center = 0.5 # for calculating J integral`
- 逐行解释：这一行执行赋值，把右侧表达式 `0.5 # for calculating J integral` 的结果保存到变量 `cirle_center` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ x_c = 0.5 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 35 个执行点，解释顺序与源码顺序保持一致。

#### 第 128 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“材料、损伤、载荷与几何参数初始化阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“材料、损伤、载荷与几何参数初始化阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 36 个执行点，解释顺序与源码顺序保持一致。

#### 第 129 行
- 该行代码：`crack_thickness = 0.04`
- 逐行解释：这一行执行赋值，把右侧表达式 `0.04` 的结果保存到变量 `crack_thickness` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ h_{\mathrm{crack}} = 0.04 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 37 个执行点，解释顺序与源码顺序保持一致。

#### 第 130 行
- 该行代码：`crack_tip = 0.2`
- 逐行解释：这一行执行赋值，把右侧表达式 `0.2` 的结果保存到变量 `crack_tip` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ a_0 = 0.2 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 38 个执行点，解释顺序与源码顺序保持一致。

#### 第 131 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“材料、损伤、载荷与几何参数初始化阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“材料、损伤、载荷与几何参数初始化阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 39 个执行点，解释顺序与源码顺序保持一致。

#### 第 132 行
- 该行代码：`g_power = 0.28`
- 逐行解释：这一行执行赋值，把右侧表达式 `0.28` 的结果保存到变量 `g_power` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ g_{\mathrm{power}} = 0.28 ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 40 个执行点，解释顺序与源码顺序保持一致。

#### 第 133 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“材料、损伤、载荷与几何参数初始化阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“材料、损伤、载荷与几何参数初始化阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 41 个执行点，解释顺序与源码顺序保持一致。

#### 第 134 行
- 该行代码：`# Naming parameters for saving output`
- 逐行解释：这是注释行，作用是给“材料、损伤、载荷与几何参数初始化阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 42 个执行点，解释顺序与源码顺序保持一致。

#### 第 135 行
- 该行代码：`simulation_params = "eta_%0.1f_ell_%0.2f_Lmbda_ch_max_%0.2f_g_%0.2f_lb_1.5" % (eta, ell, Lmbda_ch_max, g_power)`
- 逐行解释：用关键参数拼出输出目录名，便于批量计算后仅从文件夹名就能识别是哪组参数。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ simulation_params = "eta_%0.1f_ell_%0.2f_Lmbda_ch_max_%0.2f_g_%0.2f_lb_1.5" % (eta, ell, Lmbda_ch_max, g_power) ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 43 个执行点，解释顺序与源码顺序保持一致。

#### 第 136 行
- 该行代码：`savedir   = simulation_params + "/"`
- 逐行解释：这一行执行赋值，把右侧表达式 `simulation_params + "/"` 的结果保存到变量 `savedir` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“材料、损伤、载荷与几何参数初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ savedir = simulation_params + "/" ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 44 个执行点，解释顺序与源码顺序保持一致。

#### 第 137 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“材料、损伤、载荷与几何参数初始化阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“材料、损伤、载荷与几何参数初始化阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“材料、损伤、载荷与几何参数初始化阶段”内部的第 45 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 95 行对应公式：[ \lambda_i = 1.2 ]
- 第 96 行对应公式：[ \lambda_{\mathrm{ch}}^{\max} = 2.7 ]
- 第 97 行对应公式：[ \alpha_{\mathrm{damage}} = 1 ]
- 第 98 行对应公式：[ \beta_{\mathrm{damage}} = 20 ]
- 第 101 行对应公式：[ \mu = 1 ]
- 第 102 行对应公式：[ \kappa = 1000 ]
- 第 103 行对应公式：[ k_{\ell} = 10^{-5} ]
- 第 104 行对应公式：[ \eta = 0 ]
- 第 107 行对应公式：[ \mathbf{b} = (0,0) ]
- 第 108 行对应公式：[ t_{\min} = 0 ]

## 分段 08：第 138-169 行 —— 几何域与网格生成阶段

- 本段定位：构造矩形区域、圆环子域和多条水平窄条子域，再生成网格并把网格图保存出来。
- 与前后关系：本段结束后会进入“特征线与特征点输出阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
138: # Define the geometric domains
139: domain = Rectangle(Point(0.0, 0.0), Point(L, H))
140: domain.set_subdomain(1, Circle(Point(cirle_center, 0.5*H), r2) - Circle(Point(cirle_center, 0.5*H), r1))
141: 
142: domain.set_subdomain(2, Rectangle(Point(0, 0.47*H), Point(L, 0.475*H)))
143: domain.set_subdomain(3, Rectangle(Point(0, 0.475*H), Point(L, 0.48*H)))
144: domain.set_subdomain(4, Rectangle(Point(0, 0.48*H), Point(L, 0.485*H)))
145: domain.set_subdomain(5, Rectangle(Point(0, 0.485*H), Point(L, 0.49*H)))
146: domain.set_subdomain(6, Rectangle(Point(0, 0.49*H), Point(L, 0.495*H)))
147: domain.set_subdomain(7, Rectangle(Point(0, 0.495*H), Point(L, 0.5*H)))
148: domain.set_subdomain(8, Rectangle(Point(0, 0.5*H), Point(L, 0.505*H)))
149: domain.set_subdomain(9, Rectangle(Point(0, 0.505*H), Point(L, 0.51*H)))
150: domain.set_subdomain(10, Rectangle(Point(0, 0.51*H), Point(L, 0.515*H)))
151: domain.set_subdomain(11, Rectangle(Point(0, 0.515*H), Point(L, 0.52*H)))
152: domain.set_subdomain(12, Rectangle(Point(0, 0.52*H), Point(L, 0.525*H)))
153: domain.set_subdomain(13, Rectangle(Point(0, 0.525*H), Point(L, 0.53*H)))
154: 
155: mesh = generate_mesh(domain, resolution)
156: boundary_markers = MeshFunction("size_t", mesh, mesh.topology().dim() - 1, mesh.domains())
157: top_boundary.mark(boundary_markers, 1)
158: ds = Measure('ds', domain=mesh, subdomain_data=boundary_markers)
159: mf = MeshFunction("size_t", mesh, 2, mesh.domains())
160: dx = Measure("dx", domain=mesh, subdomain_data=mf)
161: 
162: # Plot and save mesh
163: if not os.path.exists(savedir):
164:     os.makedirs(savedir)
165: plt.figure()
166: plot(mesh)
167: plt.savefig(savedir + "mesh.pdf")
168: plt.close()
169: 
```

### 分段内逐行解释

#### 第 138 行
- 该行代码：`# Define the geometric domains`
- 逐行解释：这是注释行，作用是给“几何域与网格生成阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 139 行
- 该行代码：`domain = Rectangle(Point(0.0, 0.0), Point(L, H))`
- 逐行解释：主计算域是一个矩形，接下来作者继续在其中嵌入环带和多条窄矩形子域，用于积分和观察。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Omega = [0,L]\times[0,H] ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 140 行
- 该行代码：`domain.set_subdomain(1, Circle(Point(cirle_center, 0.5*H), r2) - Circle(Point(cirle_center, 0.5*H), r1))`
- 逐行解释：子域 1 是一个圆环，它正是后面计算 J 积分时要积分的区域，因此先在几何上单独标出来。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Omega_{J} = B_{r_2}(x_c,H/2) \setminus B_{r_1}(x_c,H/2) ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 141 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“几何域与网格生成阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“几何域与网格生成阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 142 行
- 该行代码：`domain.set_subdomain(2, Rectangle(Point(0, 0.47*H), Point(L, 0.475*H)))`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 143 行
- 该行代码：`domain.set_subdomain(3, Rectangle(Point(0, 0.475*H), Point(L, 0.48*H)))`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 144 行
- 该行代码：`domain.set_subdomain(4, Rectangle(Point(0, 0.48*H), Point(L, 0.485*H)))`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 145 行
- 该行代码：`domain.set_subdomain(5, Rectangle(Point(0, 0.485*H), Point(L, 0.49*H)))`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 146 行
- 该行代码：`domain.set_subdomain(6, Rectangle(Point(0, 0.49*H), Point(L, 0.495*H)))`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 147 行
- 该行代码：`domain.set_subdomain(7, Rectangle(Point(0, 0.495*H), Point(L, 0.5*H)))`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 148 行
- 该行代码：`domain.set_subdomain(8, Rectangle(Point(0, 0.5*H), Point(L, 0.505*H)))`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 149 行
- 该行代码：`domain.set_subdomain(9, Rectangle(Point(0, 0.505*H), Point(L, 0.51*H)))`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 150 行
- 该行代码：`domain.set_subdomain(10, Rectangle(Point(0, 0.51*H), Point(L, 0.515*H)))`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 151 行
- 该行代码：`domain.set_subdomain(11, Rectangle(Point(0, 0.515*H), Point(L, 0.52*H)))`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 152 行
- 该行代码：`domain.set_subdomain(12, Rectangle(Point(0, 0.52*H), Point(L, 0.525*H)))`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 153 行
- 该行代码：`domain.set_subdomain(13, Rectangle(Point(0, 0.525*H), Point(L, 0.53*H)))`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 154 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“几何域与网格生成阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“几何域与网格生成阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 155 行
- 该行代码：`mesh = generate_mesh(domain, resolution)`
- 逐行解释：到这里才真正把几何连续体离散成有限元网格，后续所有空间、函数和积分都依赖这个 `mesh`。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathcal{T}_h = \mathrm{generate\_mesh}(\Omega,\,\mathrm{resolution}) ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

#### 第 156 行
- 该行代码：`boundary_markers = MeshFunction("size_t", mesh, mesh.topology().dim() - 1, mesh.domains())`
- 逐行解释：这一行执行赋值，把右侧表达式 `MeshFunction("size_t", mesh, mesh.topology().dim() - 1, mesh.domains())` 的结果保存到变量 `boundary_markers` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ boundary_markers = MeshFunction("size_t", mesh, mesh.topology().dim() - 1, mesh.domains()) ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 19 个执行点，解释顺序与源码顺序保持一致。

#### 第 157 行
- 该行代码：`top_boundary.mark(boundary_markers, 1)`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 20 个执行点，解释顺序与源码顺序保持一致。

#### 第 158 行
- 该行代码：`ds = Measure('ds', domain=mesh, subdomain_data=boundary_markers)`
- 逐行解释：这一行执行赋值，把右侧表达式 `Measure('ds', domain=mesh, subdomain_data=boundary_markers)` 的结果保存到变量 `ds` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathrm{d}s = \mathrm{Measure}(\Gamma) ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 21 个执行点，解释顺序与源码顺序保持一致。

#### 第 159 行
- 该行代码：`mf = MeshFunction("size_t", mesh, 2, mesh.domains())`
- 逐行解释：这一行执行赋值，把右侧表达式 `MeshFunction("size_t", mesh, 2, mesh.domains())` 的结果保存到变量 `mf` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ mf = MeshFunction("size_t", mesh, 2, mesh.domains()) ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 22 个执行点，解释顺序与源码顺序保持一致。

#### 第 160 行
- 该行代码：`dx = Measure("dx", domain=mesh, subdomain_data=mf)`
- 逐行解释：这一行执行赋值，把右侧表达式 `Measure("dx", domain=mesh, subdomain_data=mf)` 的结果保存到变量 `dx` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathrm{d}x = \mathrm{Measure}(\Omega) ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 23 个执行点，解释顺序与源码顺序保持一致。

#### 第 161 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“几何域与网格生成阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“几何域与网格生成阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 24 个执行点，解释顺序与源码顺序保持一致。

#### 第 162 行
- 该行代码：`# Plot and save mesh`
- 逐行解释：这是注释行，作用是给“几何域与网格生成阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 25 个执行点，解释顺序与源码顺序保持一致。

#### 第 163 行
- 该行代码：`if not os.path.exists(savedir):`
- 逐行解释：如果输出目录不存在就创建，这是一个纯 I/O 保护步骤，保证后面的网格图和结果文件都能顺利写出。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行体现条件逻辑，而非新的数学算子定义。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 26 个执行点，解释顺序与源码顺序保持一致。

#### 第 164 行
- 该行代码：`    os.makedirs(savedir)`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 27 个执行点，解释顺序与源码顺序保持一致。

#### 第 165 行
- 该行代码：`plt.figure()`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 28 个执行点，解释顺序与源码顺序保持一致。

#### 第 166 行
- 该行代码：`plot(mesh)`
- 逐行解释：这一行位于“几何域与网格生成阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这一行不会反馈到求解器，但会把前面已经得到的数据转成可视化结果，方便人工验算。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 29 个执行点，解释顺序与源码顺序保持一致。

#### 第 167 行
- 该行代码：`plt.savefig(savedir + "mesh.pdf")`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这一行不会反馈到求解器，但会把前面已经得到的数据转成可视化结果，方便人工验算。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 30 个执行点，解释顺序与源码顺序保持一致。

#### 第 168 行
- 该行代码：`plt.close()`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这行执行后会在“几何域与网格生成阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 31 个执行点，解释顺序与源码顺序保持一致。

#### 第 169 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“几何域与网格生成阶段”内部的第 32 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 139 行对应公式：[ \Omega = [0,L]\times[0,H] ]
- 第 140 行对应公式：[ \Omega_{J} = B_{r_2}(x_c,H/2) \setminus B_{r_1}(x_c,H/2) ]
- 第 155 行对应公式：[ \mathcal{T}_h = \mathrm{generate\_mesh}(\Omega,\,\mathrm{resolution}) ]
- 第 156 行对应公式：[ boundary_markers = MeshFunction("size_t", mesh, mesh.topology().dim() - 1, mesh.domains()) ]
- 第 158 行对应公式：[ \mathrm{d}s = \mathrm{Measure}(\Gamma) ]
- 第 159 行对应公式：[ mf = MeshFunction("size_t", mesh, 2, mesh.domains()) ]
- 第 160 行对应公式：[ \mathrm{d}x = \mathrm{Measure}(\Omega) ]

## 分段 09：第 170-187 行 —— 特征线与特征点输出阶段

- 本段定位：把感兴趣的边界线和点标记出来并写入 XDMF，便于可视化检查几何定义是否正确。
- 与前后关系：本段结束后会进入“有限元空间与 J 积分辅助函数构造阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
170: #-----------------------------------------------------------------------------
171: # Define lines and points
172: lines = MeshFunction("size_t", mesh, mesh.topology().dim() - 1)
173: points = MeshFunction("size_t", mesh, mesh.topology().dim() - 2)
174: 
175: # show lines of interest
176: lines.set_all(0)
177: bot_boundary.mark(lines, 1)
178: top_boundary.mark(lines, 1)
179: file_results = XDMFFile(savedir + "/" + "lines.xdmf")
180: file_results.write(lines)
181: 
182: # Show points of interest
183: points.set_all(0)
184: pin_point.mark(points, 1)
185: file_results = XDMFFile(savedir + "/" + "points.xdmf")
186: file_results.write(points)
187: 
```

### 分段内逐行解释

#### 第 170 行
- 该行代码：`#-----------------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“特征线与特征点输出阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 171 行
- 该行代码：`# Define lines and points`
- 逐行解释：这是注释行，作用是给“特征线与特征点输出阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 172 行
- 该行代码：`lines = MeshFunction("size_t", mesh, mesh.topology().dim() - 1)`
- 逐行解释：这里额外建立线标记数据结构，用于把顶边界和底边界写出到 XDMF，方便在 ParaView 中检查边界是否标错。
- 运行影响：这行执行后会在“特征线与特征点输出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ lines = MeshFunction("size_t", mesh, mesh.topology().dim() - 1) ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 173 行
- 该行代码：`points = MeshFunction("size_t", mesh, mesh.topology().dim() - 2)`
- 逐行解释：这一行执行赋值，把右侧表达式 `MeshFunction("size_t", mesh, mesh.topology().dim() - 2)` 的结果保存到变量 `points` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“特征线与特征点输出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ points = MeshFunction("size_t", mesh, mesh.topology().dim() - 2) ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 174 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“特征线与特征点输出阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“特征线与特征点输出阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 175 行
- 该行代码：`# show lines of interest`
- 逐行解释：这是注释行，作用是给“特征线与特征点输出阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 176 行
- 该行代码：`lines.set_all(0)`
- 逐行解释：这一行位于“特征线与特征点输出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“特征线与特征点输出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 177 行
- 该行代码：`bot_boundary.mark(lines, 1)`
- 逐行解释：这一行位于“特征线与特征点输出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“特征线与特征点输出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 178 行
- 该行代码：`top_boundary.mark(lines, 1)`
- 逐行解释：这一行位于“特征线与特征点输出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“特征线与特征点输出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 179 行
- 该行代码：`file_results = XDMFFile(savedir + "/" + "lines.xdmf")`
- 逐行解释：这一行执行赋值，把右侧表达式 `XDMFFile(savedir + "/" + "lines.xdmf")` 的结果保存到变量 `file_results` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“特征线与特征点输出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ file_results = XDMFFile(savedir + "/" + "lines.xdmf") ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 180 行
- 该行代码：`file_results.write(lines)`
- 逐行解释：这一行把当前网格函数或解场写入可视化文件，便于在 ParaView 等软件中按时间步查看结果。
- 运行影响：这行执行后会在“特征线与特征点输出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 181 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“特征线与特征点输出阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“特征线与特征点输出阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 182 行
- 该行代码：`# Show points of interest`
- 逐行解释：这是注释行，作用是给“特征线与特征点输出阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 183 行
- 该行代码：`points.set_all(0)`
- 逐行解释：这一行位于“特征线与特征点输出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“特征线与特征点输出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 184 行
- 该行代码：`pin_point.mark(points, 1)`
- 逐行解释：这一行位于“特征线与特征点输出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“特征线与特征点输出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 185 行
- 该行代码：`file_results = XDMFFile(savedir + "/" + "points.xdmf")`
- 逐行解释：这一行执行赋值，把右侧表达式 `XDMFFile(savedir + "/" + "points.xdmf")` 的结果保存到变量 `file_results` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“特征线与特征点输出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ file_results = XDMFFile(savedir + "/" + "points.xdmf") ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 186 行
- 该行代码：`file_results.write(points)`
- 逐行解释：这一行把当前网格函数或解场写入可视化文件，便于在 ParaView 等软件中按时间步查看结果。
- 运行影响：这行执行后会在“特征线与特征点输出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 187 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“特征线与特征点输出阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 172 行对应公式：[ lines = MeshFunction("size_t", mesh, mesh.topology().dim() - 1) ]
- 第 173 行对应公式：[ points = MeshFunction("size_t", mesh, mesh.topology().dim() - 2) ]
- 第 179 行对应公式：[ file_results = XDMFFile(savedir + "/" + "lines.xdmf") ]
- 第 185 行对应公式：[ file_results = XDMFFile(savedir + "/" + "points.xdmf") ]

## 分段 10：第 188-230 行 —— 有限元空间与 J 积分辅助函数构造阶段

- 本段定位：建立张量、标量、混合有限元空间，并构造 J 积分权函数 q。
- 与前后关系：本段结束后会进入“位移与伸长边界条件设置阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
188: # Variational formulation
189: # ----------------------------------------------------------------------------
190: # Tensor space for projection of stress
191: T_DG0 = TensorFunctionSpace(mesh,'DG',0)
192: DG0   = FunctionSpace(mesh,'DG',0)
193: # Create mixed function space for elasticity
194: V_CG2 = VectorFunctionSpace(mesh, "Lagrange", 2)
195: # CG1 also defines the function space for damage
196: CG1 = FunctionSpace(mesh, "Lagrange", 1)
197: V_CG2elem = V_CG2.ufl_element()
198: CG1elem = CG1.ufl_element()
199: # Stabilized mixed FEM for incompressible elasticity
200: MixElem = MixedElement([V_CG2elem, CG1elem])
201: # Define function spaces for displacement and pressure
202: V = FunctionSpace(mesh, MixElem)
203: 
204: # Define the function, test and trial fields
205: w_p = Function(V)
206: u_p = TrialFunction(V)
207: v_q = TestFunction(V)
208: (u, p) = split(w_p)     # Displacement, pressure
209: (v, q) = split(v_q)   # Test functions for u, p
210: # Define the function, test and trial fields for damage problem
211: Lmbda  = Function(CG1)
212: Lmbda_trial = TrialFunction(CG1)
213: beta   = TestFunction(CG1)
214: damage = Function(CG1)
215: damage_previous = Function(CG1)
216: 
217: # Define functions to save
218: PTensor = Function(T_DG0, name="Nominal Stress")
219: FTensor = Function(T_DG0, name="Deformation Gradient")
220: JScalar = Function(CG1, name="Volume Ratio")
221: 
222: #--------------------------------------------------------------------
223: q_func_space = FunctionSpace(mesh, 'CG', 1)
224: q_expr = Expression('sqrt(pow(x[0]-cirle_center, 2) + pow(x[1]-0.5*H, 2)) < r1 ? \
225:                      1.0 : (sqrt(pow(x[0]-cirle_center, 2) + pow(x[1]-0.5*H, 2)) > r2 ? 0.0 : \
226:                      (r2 - sqrt(pow(x[0]-cirle_center, 2) + pow(x[1]-0.5*H, 2))) / (r2 - r1))', \
227:                           degree=1, r1=r1, r2=r2, cirle_center=cirle_center, H=H)
228: q_J_integral = interpolate(q_expr, q_func_space)
229: q_output = File(savedir+"q_J_integral.pvd")
230: q_output << q_J_integral
```

### 分段内逐行解释

#### 第 188 行
- 该行代码：`# Variational formulation`
- 逐行解释：这是注释行，作用是给“有限元空间与 J 积分辅助函数构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 189 行
- 该行代码：`# ----------------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“有限元空间与 J 积分辅助函数构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 190 行
- 该行代码：`# Tensor space for projection of stress`
- 逐行解释：这是注释行，作用是给“有限元空间与 J 积分辅助函数构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 191 行
- 该行代码：`T_DG0 = TensorFunctionSpace(mesh,'DG',0)`
- 逐行解释：定义分片常数张量空间，是因为名义应力和变形梯度在单元层面输出时，用 DG0 空间既稳定又节省存储。
- 运行影响：这一行一旦执行，就确定了某类未知量或投影量的离散空间，从而决定后续向量长度、插值阶次和输出精度。
- 公式/符号：[ \mathbf{T}_{DG0} = \mathrm{DG}_0^{2\times 2} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 192 行
- 该行代码：`DG0   = FunctionSpace(mesh,'DG',0)`
- 逐行解释：这一行执行赋值，把右侧表达式 `FunctionSpace(mesh,'DG',0)` 的结果保存到变量 `DG0` 中；后续主流程会直接引用这个量。
- 运行影响：这一行一旦执行，就确定了某类未知量或投影量的离散空间，从而决定后续向量长度、插值阶次和输出精度。
- 公式/符号：[ DG0 = \mathrm{DG}_0 ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 193 行
- 该行代码：`# Create mixed function space for elasticity`
- 逐行解释：这是注释行，作用是给“有限元空间与 J 积分辅助函数构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 194 行
- 该行代码：`V_CG2 = VectorFunctionSpace(mesh, "Lagrange", 2)`
- 逐行解释：位移采用二次连续元，可以更准确地表示大变形场；这是混合元中位移部分的高阶近似。
- 运行影响：这一行一旦执行，就确定了某类未知量或投影量的离散空间，从而决定后续向量长度、插值阶次和输出精度。
- 公式/符号：[ \mathbf{V}_{CG2} = [CG_2]^2 ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 195 行
- 该行代码：`# CG1 also defines the function space for damage`
- 逐行解释：这是注释行，作用是给“有限元空间与 J 积分辅助函数构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 196 行
- 该行代码：`CG1 = FunctionSpace(mesh, "Lagrange", 1)`
- 逐行解释：损伤/伸长变量使用一次连续元，兼顾计算效率与梯度项所需的连续性。
- 运行影响：这一行一旦执行，就确定了某类未知量或投影量的离散空间，从而决定后续向量长度、插值阶次和输出精度。
- 公式/符号：[ CG1 = CG_1 ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 197 行
- 该行代码：`V_CG2elem = V_CG2.ufl_element()`
- 逐行解释：这一行执行赋值，把右侧表达式 `V_CG2.ufl_element()` 的结果保存到变量 `V_CG2elem` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ V_CG2elem = V_CG2.ufl_element() ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 198 行
- 该行代码：`CG1elem = CG1.ufl_element()`
- 逐行解释：这一行执行赋值，把右侧表达式 `CG1.ufl_element()` 的结果保存到变量 `CG1elem` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ CG1elem = CG1.ufl_element() ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 199 行
- 该行代码：`# Stabilized mixed FEM for incompressible elasticity`
- 逐行解释：这是注释行，作用是给“有限元空间与 J 积分辅助函数构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 200 行
- 该行代码：`MixElem = MixedElement([V_CG2elem, CG1elem])`
- 逐行解释：把位移元和压力元混成一个 MixedElement，是典型的不可压缩弹性混合有限元写法。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathcal{V} = [CG_2]^2 \times CG_1 ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 201 行
- 该行代码：`# Define function spaces for displacement and pressure`
- 逐行解释：这是注释行，作用是给“有限元空间与 J 积分辅助函数构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 202 行
- 该行代码：`V = FunctionSpace(mesh, MixElem)`
- 逐行解释：这一行执行赋值，把右侧表达式 `FunctionSpace(mesh, MixElem)` 的结果保存到变量 `V` 中；后续主流程会直接引用这个量。
- 运行影响：这一行一旦执行，就确定了某类未知量或投影量的离散空间，从而决定后续向量长度、插值阶次和输出精度。
- 公式/符号：[ V = \mathbf{V}_{CG2} \times CG1 ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 203 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“有限元空间与 J 积分辅助函数构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“有限元空间与 J 积分辅助函数构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 204 行
- 该行代码：`# Define the function, test and trial fields`
- 逐行解释：这是注释行，作用是给“有限元空间与 J 积分辅助函数构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 205 行
- 该行代码：`w_p = Function(V)`
- 逐行解释：从这一行起建立混合未知量 `w_p`，它同时携带位移和压力，后面求解器只需要操作这一个场。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ w_p = Function(V) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

#### 第 206 行
- 该行代码：`u_p = TrialFunction(V)`
- 逐行解释：这一行执行赋值，把右侧表达式 `TrialFunction(V)` 的结果保存到变量 `u_p` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ u_p = TrialFunction(V) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 19 个执行点，解释顺序与源码顺序保持一致。

#### 第 207 行
- 该行代码：`v_q = TestFunction(V)`
- 逐行解释：这一行执行赋值，把右侧表达式 `TestFunction(V)` 的结果保存到变量 `v_q` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ v_q = TestFunction(V) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 20 个执行点，解释顺序与源码顺序保持一致。

#### 第 208 行
- 该行代码：`(u, p) = split(w_p)     # Displacement, pressure`
- 逐行解释：这一行执行赋值，把右侧表达式 `split(w_p)     # Displacement, pressure` 的结果保存到变量 `(u, p)` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ (\mathbf{u},p) = \mathrm{split}(w_p) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 21 个执行点，解释顺序与源码顺序保持一致。

#### 第 209 行
- 该行代码：`(v, q) = split(v_q)   # Test functions for u, p`
- 逐行解释：这一行执行赋值，把右侧表达式 `split(v_q)   # Test functions for u, p` 的结果保存到变量 `(v, q)` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ (\mathbf{v},q) = \mathrm{split}(v_q) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 22 个执行点，解释顺序与源码顺序保持一致。

#### 第 210 行
- 该行代码：`# Define the function, test and trial fields for damage problem`
- 逐行解释：这是注释行，作用是给“有限元空间与 J 积分辅助函数构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 23 个执行点，解释顺序与源码顺序保持一致。

#### 第 211 行
- 该行代码：`Lmbda  = Function(CG1)`
- 逐行解释：这一行执行赋值，把右侧表达式 `Function(CG1)` 的结果保存到变量 `Lmbda` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ Lmbda = Function(CG1) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 24 个执行点，解释顺序与源码顺序保持一致。

#### 第 212 行
- 该行代码：`Lmbda_trial = TrialFunction(CG1)`
- 逐行解释：这一行执行赋值，把右侧表达式 `TrialFunction(CG1)` 的结果保存到变量 `Lmbda_trial` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ Lmbda_trial = TrialFunction(CG1) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 25 个执行点，解释顺序与源码顺序保持一致。

#### 第 213 行
- 该行代码：`beta   = TestFunction(CG1)`
- 逐行解释：这一行执行赋值，把右侧表达式 `TestFunction(CG1)` 的结果保存到变量 `beta` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ beta = TestFunction(CG1) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 26 个执行点，解释顺序与源码顺序保持一致。

#### 第 214 行
- 该行代码：`damage = Function(CG1)`
- 逐行解释：这一行执行赋值，把右侧表达式 `Function(CG1)` 的结果保存到变量 `damage` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ damage = Function(CG1) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 27 个执行点，解释顺序与源码顺序保持一致。

#### 第 215 行
- 该行代码：`damage_previous = Function(CG1)`
- 逐行解释：这一行执行赋值，把右侧表达式 `Function(CG1)` 的结果保存到变量 `damage_previous` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ damage_previous = Function(CG1) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 28 个执行点，解释顺序与源码顺序保持一致。

#### 第 216 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“有限元空间与 J 积分辅助函数构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“有限元空间与 J 积分辅助函数构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 29 个执行点，解释顺序与源码顺序保持一致。

#### 第 217 行
- 该行代码：`# Define functions to save`
- 逐行解释：这是注释行，作用是给“有限元空间与 J 积分辅助函数构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 30 个执行点，解释顺序与源码顺序保持一致。

#### 第 218 行
- 该行代码：`PTensor = Function(T_DG0, name="Nominal Stress")`
- 逐行解释：这一行执行赋值，把右侧表达式 `Function(T_DG0, name="Nominal Stress")` 的结果保存到变量 `PTensor` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ PTensor = Function(T_DG0, name="Nominal Stress") ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 31 个执行点，解释顺序与源码顺序保持一致。

#### 第 219 行
- 该行代码：`FTensor = Function(T_DG0, name="Deformation Gradient")`
- 逐行解释：这一行执行赋值，把右侧表达式 `Function(T_DG0, name="Deformation Gradient")` 的结果保存到变量 `FTensor` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ FTensor = Function(T_DG0, name="Deformation Gradient") ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 32 个执行点，解释顺序与源码顺序保持一致。

#### 第 220 行
- 该行代码：`JScalar = Function(CG1, name="Volume Ratio")`
- 逐行解释：这一行执行赋值，把右侧表达式 `Function(CG1, name="Volume Ratio")` 的结果保存到变量 `JScalar` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ JScalar = Function(CG1, name="Volume Ratio") ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 33 个执行点，解释顺序与源码顺序保持一致。

#### 第 221 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“有限元空间与 J 积分辅助函数构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“有限元空间与 J 积分辅助函数构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 34 个执行点，解释顺序与源码顺序保持一致。

#### 第 222 行
- 该行代码：`#--------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“有限元空间与 J 积分辅助函数构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 35 个执行点，解释顺序与源码顺序保持一致。

#### 第 223 行
- 该行代码：`q_func_space = FunctionSpace(mesh, 'CG', 1)`
- 逐行解释：这一行执行赋值，把右侧表达式 `FunctionSpace(mesh, 'CG', 1)` 的结果保存到变量 `q_func_space` 中；后续主流程会直接引用这个量。
- 运行影响：这一行一旦执行，就确定了某类未知量或投影量的离散空间，从而决定后续向量长度、插值阶次和输出精度。
- 公式/符号：[ q_func_space = FunctionSpace(mesh, 'CG', 1) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 36 个执行点，解释顺序与源码顺序保持一致。

#### 第 224 行
- 该行代码：`q_expr = Expression('sqrt(pow(x[0]-cirle_center, 2) + pow(x[1]-0.5*H, 2)) < r1 ? \`
- 逐行解释：J 积分辅助函数 q 被定义成一个径向分段函数：内圈为 1，外圈为 0，中间做线性过渡，这正是域形式 J 积分的标准权函数。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ q(\mathbf{x}) = \begin{cases}1, & r < r_1 \\ 0, & r > r_2 \\ \dfrac{r_2-r}{r_2-r_1}, & r_1\le r \le r_2 \end{cases} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 37 个执行点，解释顺序与源码顺序保持一致。

#### 第 225 行
- 该行代码：`                     1.0 : (sqrt(pow(x[0]-cirle_center, 2) + pow(x[1]-0.5*H, 2)) > r2 ? 0.0 : \`
- 逐行解释：这一行位于“有限元空间与 J 积分辅助函数构造阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 38 个执行点，解释顺序与源码顺序保持一致。

#### 第 226 行
- 该行代码：`                     (r2 - sqrt(pow(x[0]-cirle_center, 2) + pow(x[1]-0.5*H, 2))) / (r2 - r1))', \`
- 逐行解释：这一行位于“有限元空间与 J 积分辅助函数构造阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 39 个执行点，解释顺序与源码顺序保持一致。

#### 第 227 行
- 该行代码：`                          degree=1, r1=r1, r2=r2, cirle_center=cirle_center, H=H)`
- 逐行解释：这一行执行赋值，把右侧表达式 `1, r1=r1, r2=r2, cirle_center=cirle_center, H=H)` 的结果保存到变量 `degree` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ degree = 1, r1=r1, r2=r2, cirle_center=cirle_center, H=H) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 40 个执行点，解释顺序与源码顺序保持一致。

#### 第 228 行
- 该行代码：`q_J_integral = interpolate(q_expr, q_func_space)`
- 逐行解释：这一行执行赋值，把右侧表达式 `interpolate(q_expr, q_func_space)` 的结果保存到变量 `q_J_integral` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ q_J = \Pi_{CG1}\,q(\mathbf{x}) ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 41 个执行点，解释顺序与源码顺序保持一致。

#### 第 229 行
- 该行代码：`q_output = File(savedir+"q_J_integral.pvd")`
- 逐行解释：这一行执行赋值，把右侧表达式 `File(savedir+"q_J_integral.pvd")` 的结果保存到变量 `q_output` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ q_output = File(savedir+"q_J_integral.pvd") ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 42 个执行点，解释顺序与源码顺序保持一致。

#### 第 230 行
- 该行代码：`q_output << q_J_integral`
- 逐行解释：这一行位于“有限元空间与 J 积分辅助函数构造阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“有限元空间与 J 积分辅助函数构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“有限元空间与 J 积分辅助函数构造阶段”内部的第 43 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 191 行对应公式：[ \mathbf{T}_{DG0} = \mathrm{DG}_0^{2\times 2} ]
- 第 192 行对应公式：[ DG0 = \mathrm{DG}_0 ]
- 第 194 行对应公式：[ \mathbf{V}_{CG2} = [CG_2]^2 ]
- 第 196 行对应公式：[ CG1 = CG_1 ]
- 第 197 行对应公式：[ V_CG2elem = V_CG2.ufl_element() ]
- 第 198 行对应公式：[ CG1elem = CG1.ufl_element() ]
- 第 200 行对应公式：[ \mathcal{V} = [CG_2]^2 \times CG_1 ]
- 第 202 行对应公式：[ V = \mathbf{V}_{CG2} \times CG1 ]
- 第 205 行对应公式：[ w_p = Function(V) ]
- 第 206 行对应公式：[ u_p = TrialFunction(V) ]

## 分段 11：第 231-243 行 —— 位移与伸长边界条件设置阶段

- 本段定位：对顶底边界施加位移加载，同时把链伸长变量在上下边界固定为 1。
- 与前后关系：本段结束后会进入“运动学量、应力函数与弹性问题构造阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
231: 
232: # Dirichlet boundary condition
233: # --------------------------------------------------------------------
234: u1 = Expression([0, "(-t/L)*x[0] + t"], t=0.0, L=L, degree=1)
235: u2 = Expression([0, "(t/L)*x[0] - t"], t=0.0, L=L, degree=1)
236: bc_u1 = DirichletBC(V.sub(0), u1, top_boundary)
237: bc_u2 = DirichletBC(V.sub(0), u2, bot_boundary)
238: bc_u = [bc_u1, bc_u2]
239: 
240: # bc - alpha (zero damage)
241: bc_Lmbda_B = DirichletBC(CG1, 1, bot_boundary)
242: bc_Lmbda_T = DirichletBC(CG1, 1, top_boundary)
243: bc_Lmbda = [bc_Lmbda_B, bc_Lmbda_T]
```

### 分段内逐行解释

#### 第 231 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 232 行
- 该行代码：`# Dirichlet boundary condition`
- 逐行解释：这是注释行，作用是给“位移与伸长边界条件设置阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 233 行
- 该行代码：`# --------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“位移与伸长边界条件设置阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 234 行
- 该行代码：`u1 = Expression([0, "(-t/L)*x[0] + t"], t=0.0, L=L, degree=1)`
- 逐行解释：顶边界位移表达式给出一个关于 x 的线性函数，形成剪切/张开混合的位移边界。
- 运行影响：这行执行后会在“位移与伸长边界条件设置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathbf{u}_{\mathrm{top}}(x) = (0,\,-tx/L+t) ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 235 行
- 该行代码：`u2 = Expression([0, "(t/L)*x[0] - t"], t=0.0, L=L, degree=1)`
- 逐行解释：底边界位移与顶边界相反，二者共同构造出对称加载，使中部裂纹区域受到拉伸。
- 运行影响：这行执行后会在“位移与伸长边界条件设置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathbf{u}_{\mathrm{bot}}(x) = (0,\,tx/L-t) ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 236 行
- 该行代码：`bc_u1 = DirichletBC(V.sub(0), u1, top_boundary)`
- 逐行解释：这一行执行赋值，把右侧表达式 `DirichletBC(V.sub(0), u1, top_boundary)` 的结果保存到变量 `bc_u1` 中；后续主流程会直接引用这个量。
- 运行影响：这一行把边界约束固定下来，后续求解器装配矩阵时会自动把这些条件施加到离散系统上。
- 公式/符号：[ bc_u1 = DirichletBC(V.sub(0), u1, top_boundary) ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 237 行
- 该行代码：`bc_u2 = DirichletBC(V.sub(0), u2, bot_boundary)`
- 逐行解释：这一行执行赋值，把右侧表达式 `DirichletBC(V.sub(0), u2, bot_boundary)` 的结果保存到变量 `bc_u2` 中；后续主流程会直接引用这个量。
- 运行影响：这一行把边界约束固定下来，后续求解器装配矩阵时会自动把这些条件施加到离散系统上。
- 公式/符号：[ bc_u2 = DirichletBC(V.sub(0), u2, bot_boundary) ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 238 行
- 该行代码：`bc_u = [bc_u1, bc_u2]`
- 逐行解释：这一行执行赋值，把右侧表达式 `[bc_u1, bc_u2]` 的结果保存到变量 `bc_u` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“位移与伸长边界条件设置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ bc_u = [bc_u1, bc_u2] ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 239 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“位移与伸长边界条件设置阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“位移与伸长边界条件设置阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 240 行
- 该行代码：`# bc - alpha (zero damage)`
- 逐行解释：这是注释行，作用是给“位移与伸长边界条件设置阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 241 行
- 该行代码：`bc_Lmbda_B = DirichletBC(CG1, 1, bot_boundary)`
- 逐行解释：这一行执行赋值，把右侧表达式 `DirichletBC(CG1, 1, bot_boundary)` 的结果保存到变量 `bc_Lmbda_B` 中；后续主流程会直接引用这个量。
- 运行影响：这一行把边界约束固定下来，后续求解器装配矩阵时会自动把这些条件施加到离散系统上。
- 公式/符号：[ \Lambda|_{\Gamma_{\mathrm{bot}}} = 1 ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 242 行
- 该行代码：`bc_Lmbda_T = DirichletBC(CG1, 1, top_boundary)`
- 逐行解释：这一行执行赋值，把右侧表达式 `DirichletBC(CG1, 1, top_boundary)` 的结果保存到变量 `bc_Lmbda_T` 中；后续主流程会直接引用这个量。
- 运行影响：这一行把边界约束固定下来，后续求解器装配矩阵时会自动把这些条件施加到离散系统上。
- 公式/符号：[ \Lambda|_{\Gamma_{\mathrm{top}}} = 1 ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 243 行
- 该行代码：`bc_Lmbda = [bc_Lmbda_B, bc_Lmbda_T]`
- 逐行解释：这一行执行赋值，把右侧表达式 `[bc_Lmbda_B, bc_Lmbda_T]` 的结果保存到变量 `bc_Lmbda` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“位移与伸长边界条件设置阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ bc_Lmbda = [bc_Lmbda_B, bc_Lmbda_T] ]
- 行内顺序关系：这一行属于“位移与伸长边界条件设置阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 234 行对应公式：[ \mathbf{u}_{\mathrm{top}}(x) = (0,\,-tx/L+t) ]
- 第 235 行对应公式：[ \mathbf{u}_{\mathrm{bot}}(x) = (0,\,tx/L-t) ]
- 第 236 行对应公式：[ bc_u1 = DirichletBC(V.sub(0), u1, top_boundary) ]
- 第 237 行对应公式：[ bc_u2 = DirichletBC(V.sub(0), u2, bot_boundary) ]
- 第 238 行对应公式：[ bc_u = [bc_u1, bc_u2] ]
- 第 241 行对应公式：[ \Lambda|_{\Gamma_{\mathrm{bot}}} = 1 ]
- 第 242 行对应公式：[ \Lambda|_{\Gamma_{\mathrm{top}}} = 1 ]
- 第 243 行对应公式：[ bc_Lmbda = [bc_Lmbda_B, bc_Lmbda_T] ]

## 分段 12：第 244-290 行 —— 运动学量、应力函数与弹性问题构造阶段

- 本段定位：从位移构造 F、C、J、Ic 和链伸长，并定义应力、能量密度、总势能及其残量/Jacobian。
- 与前后关系：本段结束后会进入“伸长场演化问题与约束求解器构造阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
244: 
245: # Define the energy functional of damage problem
246: # --------------------------------------------------------------------
247: # Kinematics
248: d = len(u)
249: I = Identity(d)             # Identity tensor
250: F = I + grad(u)             # Deformation gradient
251: C = F.T*F                   # Right Cauchy-Green tensor
252: 
253: # Invariants of deformation tensors
254: J  = det(F)
255: Ic = tr(C) + 1
256: Lmbda_ch = sqrt(Ic/3)
257: Lmbda_ch = conditional(sqrt(Ic/3) > Lmbda_ch_max, Lmbda_ch_max, sqrt(Ic/3))
258: 
259: # Define the energy functional of the elasticity problem
260: # ----------------------------------------------------------------------------
261: # Constitutive functions of the damage model
262: def w(damage):           # Specific energy dissipation per unit volume
263:     return damage
264: 
265: def a(damage):           # Modulation function
266:     return (1.0-damage)**2
267: 
268: def b_sq(damage):        # b(alpha) = (1-alpha)^6 therefore we define b squared
269:     return (1.0-damage)**3
270: 
271: def P(u, damage):        # Nominal stress tensor
272:     return a(damage)*mu*(F - inv(F.T)) - b_sq(damage)*p*J*inv(F.T)
273: 
274: def energy_density_function(u, damage):
275:     return a(damage)*(mu/2.0)*(Ic-3.0-2.0*ln(J)) - b_sq(damage)*p*(J-1.0) - (1./(2.*kappa))*(p**2)
276: 
277: # Elastic energy, additional terms enforce material incompressibility and regularizes the Lagrange Multiplier:
278: elastic_energy    = ((1-k_ell)*a(damage)+k_ell)*(mu/2.0)*(Ic-3.0-2.0*ln(J))*dx \
279:                     - b_sq(damage)*p*(J-1.0)*dx - 1./(2.*kappa)*p**2*dx 
280: 
281: external_work     = dot(body_force, u)*dx
282: elastic_potential = elastic_energy - external_work
283: 
284: # Compute directional derivative about w_p in the direction of v (Gradient)
285: F_u = derivative(elastic_potential, w_p, v_q)
286: J_u = derivative(F_u, w_p, u_p)
287: # Variational problem for the displacement
288: problem_u = NonlinearVariationalProblem(F_u, w_p, bc_u, J=J_u)
289: solver_u  = NonlinearVariationalSolver(problem_u)
290: solver_u.parameters.update(solver_u_parameters)
```

### 分段内逐行解释

#### 第 244 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 245 行
- 该行代码：`# Define the energy functional of damage problem`
- 逐行解释：这是注释行，作用是给“运动学量、应力函数与弹性问题构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 246 行
- 该行代码：`# --------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“运动学量、应力函数与弹性问题构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 247 行
- 该行代码：`# Kinematics`
- 逐行解释：这是注释行，作用是给“运动学量、应力函数与弹性问题构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 248 行
- 该行代码：`d = len(u)`
- 逐行解释：后续本构公式正式开始：先读取位移维数 d，再逐步构造 I、F、C、J、Ic 和特征伸长。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ d = \dim(\mathbf{u}) ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 249 行
- 该行代码：`I = Identity(d)             # Identity tensor`
- 逐行解释：这一行执行赋值，把右侧表达式 `Identity(d)             # Identity tensor` 的结果保存到变量 `I` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathbf{I} = \mathrm{Identity}(d) ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 250 行
- 该行代码：`F = I + grad(u)             # Deformation gradient`
- 逐行解释：变形梯度 F 是有限变形理论的核心量，程序后面几乎所有应力、能量和 J 积分都要用到它。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathbf{F} = \mathbf{I} + \nabla \mathbf{u} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 251 行
- 该行代码：`C = F.T*F                   # Right Cauchy-Green tensor`
- 逐行解释：这一行执行赋值，把右侧表达式 `F.T*F                   # Right Cauchy-Green tensor` 的结果保存到变量 `C` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathbf{C} = \mathbf{F}^T\mathbf{F} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 252 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“运动学量、应力函数与弹性问题构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“运动学量、应力函数与弹性问题构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 253 行
- 该行代码：`# Invariants of deformation tensors`
- 逐行解释：这是注释行，作用是给“运动学量、应力函数与弹性问题构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 254 行
- 该行代码：`J  = det(F)`
- 逐行解释：这一行执行赋值，把右侧表达式 `det(F)` 的结果保存到变量 `J` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ J = \det(\mathbf{F}) ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 255 行
- 该行代码：`Ic = tr(C) + 1`
- 逐行解释：这一行执行赋值，把右侧表达式 `tr(C) + 1` 的结果保存到变量 `Ic` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ I_c = \mathrm{tr}(\mathbf{C}) + 1 ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 256 行
- 该行代码：`Lmbda_ch = sqrt(Ic/3)`
- 逐行解释：这一行执行赋值，把右侧表达式 `sqrt(Ic/3)` 的结果保存到变量 `Lmbda_ch` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Lambda_{\mathrm{ch}} = \sqrt{I_c/3} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 257 行
- 该行代码：`Lmbda_ch = conditional(sqrt(Ic/3) > Lmbda_ch_max, Lmbda_ch_max, sqrt(Ic/3))`
- 逐行解释：把链伸长限制在上界 `Lmbda_ch_max`，是为了抑制极端拉伸下的数值爆炸。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Lambda_{\mathrm{ch}} = \min\left(\sqrt{I_c/3},\,\Lambda_{\mathrm{ch}}^{\max}\right) ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 258 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“运动学量、应力函数与弹性问题构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“运动学量、应力函数与弹性问题构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 259 行
- 该行代码：`# Define the energy functional of the elasticity problem`
- 逐行解释：这是注释行，作用是给“运动学量、应力函数与弹性问题构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 260 行
- 该行代码：`# ----------------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“运动学量、应力函数与弹性问题构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 261 行
- 该行代码：`# Constitutive functions of the damage model`
- 逐行解释：这是注释行，作用是给“运动学量、应力函数与弹性问题构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

#### 第 262 行
- 该行代码：`def w(damage):           # Specific energy dissipation per unit volume`
- 逐行解释：这里定义损伤耗散函数 w(d)，虽然本脚本里它很简单，但把它单独写成函数便于日后替换模型。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 19 个执行点，解释顺序与源码顺序保持一致。

#### 第 263 行
- 该行代码：`    return damage`
- 逐行解释：这一行把当前函数内部已经构造好的结果返回给调用方，使上层流程能够直接拿到局部计算结果。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ w(d) = d ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 20 个执行点，解释顺序与源码顺序保持一致。

#### 第 264 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“运动学量、应力函数与弹性问题构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“运动学量、应力函数与弹性问题构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 21 个执行点，解释顺序与源码顺序保持一致。

#### 第 265 行
- 该行代码：`def a(damage):           # Modulation function`
- 逐行解释：这一行定义函数 `a`，它在“运动学量、应力函数与弹性问题构造阶段”中把一段会被重复调用的数学或工具逻辑封装起来，真正执行发生在后续调用时。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 22 个执行点，解释顺序与源码顺序保持一致。

#### 第 266 行
- 该行代码：`    return (1.0-damage)**2`
- 逐行解释：这一行把当前函数内部已经构造好的结果返回给调用方，使上层流程能够直接拿到局部计算结果。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ a(d) = (1-d)^2 ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 23 个执行点，解释顺序与源码顺序保持一致。

#### 第 267 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“运动学量、应力函数与弹性问题构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“运动学量、应力函数与弹性问题构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 24 个执行点，解释顺序与源码顺序保持一致。

#### 第 268 行
- 该行代码：`def b_sq(damage):        # b(alpha) = (1-alpha)^6 therefore we define b squared`
- 逐行解释：这一行定义函数 `b_sq`，它在“运动学量、应力函数与弹性问题构造阶段”中把一段会被重复调用的数学或工具逻辑封装起来，真正执行发生在后续调用时。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 25 个执行点，解释顺序与源码顺序保持一致。

#### 第 269 行
- 该行代码：`    return (1.0-damage)**3`
- 逐行解释：这一行把当前函数内部已经构造好的结果返回给调用方，使上层流程能够直接拿到局部计算结果。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ b^2(d) = (1-d)^3 ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 26 个执行点，解释顺序与源码顺序保持一致。

#### 第 270 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“运动学量、应力函数与弹性问题构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“运动学量、应力函数与弹性问题构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 27 个执行点，解释顺序与源码顺序保持一致。

#### 第 271 行
- 该行代码：`def P(u, damage):        # Nominal stress tensor`
- 逐行解释：名义应力张量 P 的定义把损伤调制后的偏应力和压力项统一起来，是后处理和边界反力计算的关键。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 28 个执行点，解释顺序与源码顺序保持一致。

#### 第 272 行
- 该行代码：`    return a(damage)*mu*(F - inv(F.T)) - b_sq(damage)*p*J*inv(F.T)`
- 逐行解释：这一行把当前函数内部已经构造好的结果返回给调用方，使上层流程能够直接拿到局部计算结果。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathbf{P}(\mathbf{u},d) = a(d)\,\mu\,(\mathbf{F}-\mathbf{F}^{-T}) - b^2(d)\,p\,J\,\mathbf{F}^{-T} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 29 个执行点，解释顺序与源码顺序保持一致。

#### 第 273 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“运动学量、应力函数与弹性问题构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“运动学量、应力函数与弹性问题构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 30 个执行点，解释顺序与源码顺序保持一致。

#### 第 274 行
- 该行代码：`def energy_density_function(u, damage):`
- 逐行解释：能量密度函数把材料局部储能写成显式函数，后面的 J 积分直接调用它而不再重复手写公式。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 31 个执行点，解释顺序与源码顺序保持一致。

#### 第 275 行
- 该行代码：`    return a(damage)*(mu/2.0)*(Ic-3.0-2.0*ln(J)) - b_sq(damage)*p*(J-1.0) - (1./(2.*kappa))*(p**2)`
- 逐行解释：这一行把当前函数内部已经构造好的结果返回给调用方，使上层流程能够直接拿到局部计算结果。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \psi(\mathbf{u},d) = a(d)\,\frac{\mu}{2}\,(I_c-3-2\ln J) - b^2(d)\,p\,(J-1) - \frac{p^2}{2\kappa} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 32 个执行点，解释顺序与源码顺序保持一致。

#### 第 276 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“运动学量、应力函数与弹性问题构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“运动学量、应力函数与弹性问题构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 33 个执行点，解释顺序与源码顺序保持一致。

#### 第 277 行
- 该行代码：`# Elastic energy, additional terms enforce material incompressibility and regularizes the Lagrange Multiplier:`
- 逐行解释：这是注释行，作用是给“运动学量、应力函数与弹性问题构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 34 个执行点，解释顺序与源码顺序保持一致。

#### 第 278 行
- 该行代码：`elastic_energy    = ((1-k_ell)*a(damage)+k_ell)*(mu/2.0)*(Ic-3.0-2.0*ln(J))*dx \`
- 逐行解释：这一大行把总弹性能写成 UFL 形式，乘上 `dx` 后表示在整个区域上的积分泛函。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathcal{E}_{\mathrm{el}} = \int_{\Omega} \left(((1-k_{\ell})a(d)+k_{\ell})\frac{\mu}{2}(I_c-3-2\ln J) - b^2(d)\,p\,(J-1) - \frac{p^2}{2\kappa}\right)\,\mathrm{d}x ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 35 个执行点，解释顺序与源码顺序保持一致。

#### 第 279 行
- 该行代码：`                    - b_sq(damage)*p*(J-1.0)*dx - 1./(2.*kappa)*p**2*dx `
- 逐行解释：这一行位于“运动学量、应力函数与弹性问题构造阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 36 个执行点，解释顺序与源码顺序保持一致。

#### 第 280 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“运动学量、应力函数与弹性问题构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“运动学量、应力函数与弹性问题构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 37 个执行点，解释顺序与源码顺序保持一致。

#### 第 281 行
- 该行代码：`external_work     = dot(body_force, u)*dx`
- 逐行解释：这一行执行赋值，把右侧表达式 `dot(body_force, u)*dx` 的结果保存到变量 `external_work` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathcal{W}_{\mathrm{ext}} = \int_{\Omega} \mathbf{b}\cdot\mathbf{u}\,\mathrm{d}x ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 38 个执行点，解释顺序与源码顺序保持一致。

#### 第 282 行
- 该行代码：`elastic_potential = elastic_energy - external_work`
- 逐行解释：这一行执行赋值，把右侧表达式 `elastic_energy - external_work` 的结果保存到变量 `elastic_potential` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Pi(\mathbf{u},p,d) = \mathcal{E}_{\mathrm{el}} - \mathcal{W}_{\mathrm{ext}} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 39 个执行点，解释顺序与源码顺序保持一致。

#### 第 283 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“运动学量、应力函数与弹性问题构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“运动学量、应力函数与弹性问题构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 40 个执行点，解释顺序与源码顺序保持一致。

#### 第 284 行
- 该行代码：`# Compute directional derivative about w_p in the direction of v (Gradient)`
- 逐行解释：这是注释行，作用是给“运动学量、应力函数与弹性问题构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 41 个执行点，解释顺序与源码顺序保持一致。

#### 第 285 行
- 该行代码：`F_u = derivative(elastic_potential, w_p, v_q)`
- 逐行解释：对势能求一阶导得到残量，对应弱形式平衡方程；这是非线性有限元求解的标准入口。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ F_u = D\Pi(\mathbf{u},p;\mathbf{v},q) ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 42 个执行点，解释顺序与源码顺序保持一致。

#### 第 286 行
- 该行代码：`J_u = derivative(F_u, w_p, u_p)`
- 逐行解释：这一行执行赋值，把右侧表达式 `derivative(F_u, w_p, u_p)` 的结果保存到变量 `J_u` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ J_u = D F_u(\delta\mathbf{u},\delta p) ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 43 个执行点，解释顺序与源码顺序保持一致。

#### 第 287 行
- 该行代码：`# Variational problem for the displacement`
- 逐行解释：这是注释行，作用是给“运动学量、应力函数与弹性问题构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 44 个执行点，解释顺序与源码顺序保持一致。

#### 第 288 行
- 该行代码：`problem_u = NonlinearVariationalProblem(F_u, w_p, bc_u, J=J_u)`
- 逐行解释：这一行在 FEniCS 中实例化问题对象或求解器对象，把前面定义的弱式、边界条件和 Jacobian 绑定起来。
- 运行影响：执行后，脚本就具备了真正数值求解所需的对象封装；后面的 `.solve()` 会直接调用这里创建的实例。
- 公式/符号：[ problem_u = NonlinearVariationalProblem(F_u, w_p, bc_u, J=J_u) ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 45 个执行点，解释顺序与源码顺序保持一致。

#### 第 289 行
- 该行代码：`solver_u  = NonlinearVariationalSolver(problem_u)`
- 逐行解释：这一行在 FEniCS 中实例化问题对象或求解器对象，把前面定义的弱式、边界条件和 Jacobian 绑定起来。
- 运行影响：执行后，脚本就具备了真正数值求解所需的对象封装；后面的 `.solve()` 会直接调用这里创建的实例。
- 公式/符号：[ solver_u = NonlinearVariationalSolver(problem_u) ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 46 个执行点，解释顺序与源码顺序保持一致。

#### 第 290 行
- 该行代码：`solver_u.parameters.update(solver_u_parameters)`
- 逐行解释：这一行在 FEniCS 中实例化问题对象或求解器对象，把前面定义的弱式、边界条件和 Jacobian 绑定起来。
- 运行影响：这行执行后会在“运动学量、应力函数与弹性问题构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“运动学量、应力函数与弹性问题构造阶段”内部的第 47 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 248 行对应公式：[ d = \dim(\mathbf{u}) ]
- 第 249 行对应公式：[ \mathbf{I} = \mathrm{Identity}(d) ]
- 第 250 行对应公式：[ \mathbf{F} = \mathbf{I} + \nabla \mathbf{u} ]
- 第 251 行对应公式：[ \mathbf{C} = \mathbf{F}^T\mathbf{F} ]
- 第 254 行对应公式：[ J = \det(\mathbf{F}) ]
- 第 255 行对应公式：[ I_c = \mathrm{tr}(\mathbf{C}) + 1 ]
- 第 256 行对应公式：[ \Lambda_{\mathrm{ch}} = \sqrt{I_c/3} ]
- 第 257 行对应公式：[ \Lambda_{\mathrm{ch}} = \min\left(\sqrt{I_c/3},\,\Lambda_{\mathrm{ch}}^{\max}\right) ]
- 第 263 行对应公式：[ w(d) = d ]
- 第 266 行对应公式：[ a(d) = (1-d)^2 ]

## 分段 13：第 291-319 行 —— 伸长场演化问题与约束求解器构造阶段

- 本段定位：初始化链伸长历史变量，定义弛豫与粘性项，并建立带上下界的非线性求解器。
- 与前后关系：本段结束后会进入“加载序列与结果容器初始化阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
291: 
292: # Define the energy functional of damage problem
293: # --------------------------------------------------------------------
294: Lmbda_0 = interpolate(Expression("1", degree=0), CG1)  # initial (known) Lambda
295: Lmbda_previous = interpolate(Expression("1", degree=0), CG1)
296: 
297: def Relaxation(damage):
298:     return (1-damage)**g_power
299: 
300: def Viscous_Relaxation(Lmbda, Lmbda_previous):
301:     return conditional(Lmbda > Lmbda_previous, 1, 0)
302: 
303: dt = 1
304: 
305: Lmbda_WF = dt*(Lmbda*beta)*dx + Relaxation(damage)*dt*(ell**2)*inner(grad(Lmbda), grad(beta))*dx \
306:         - dt*Lmbda_ch*beta*dx + Viscous_Relaxation(Lmbda, Lmbda_previous)*eta*(Lmbda - Lmbda_previous)*beta*dx
307: 
308: Lmbda_Jacobian = derivative(Lmbda_WF, Lmbda, Lmbda_trial)
309: 
310: # Lower and upper bound
311: Lmbda_lb = interpolate(Expression("x[0]>=0 & x[0]<=L/5 & near(x[1], H/2, 0.01*hsize) ? 1.5 : 1", \
312:                        hsize = hsize, L=L, H=H, degree=0), CG1)
313: Lmbda_ub = interpolate(Expression("100", degree=0), CG1)
314: 
315: # Set up the solvers
316: problem_Lmbda = NonlinearVariationalProblem(Lmbda_WF, Lmbda, bc_Lmbda, J=Lmbda_Jacobian)
317: problem_Lmbda.set_bounds(Lmbda_lb, Lmbda_ub)
318: solver_Lmbda = NonlinearVariationalSolver(problem_Lmbda)
319: solver_Lmbda.parameters.update(solver_Lmbda_parameters)
```

### 分段内逐行解释

#### 第 291 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 292 行
- 该行代码：`# Define the energy functional of damage problem`
- 逐行解释：这是注释行，作用是给“伸长场演化问题与约束求解器构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 293 行
- 该行代码：`# --------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“伸长场演化问题与约束求解器构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 294 行
- 该行代码：`Lmbda_0 = interpolate(Expression("1", degree=0), CG1)  # initial (known) Lambda`
- 逐行解释：拉伸变量的初值设为 1，表示无额外链伸长；这也是损伤尚未启动时的参考状态。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Lambda_0(\mathbf{x}) = 1 ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 295 行
- 该行代码：`Lmbda_previous = interpolate(Expression("1", degree=0), CG1)`
- 逐行解释：这一行执行赋值，把右侧表达式 `interpolate(Expression("1", degree=0), CG1)` 的结果保存到变量 `Lmbda_previous` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Lambda_{\mathrm{prev}}(\mathbf{x}) = 1 ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 296 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“伸长场演化问题与约束求解器构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“伸长场演化问题与约束求解器构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 297 行
- 该行代码：`def Relaxation(damage):`
- 逐行解释：Relaxation(damage) 给出损伤后的弛豫能力，损伤越大，传递伸长梯度的能力越弱。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 298 行
- 该行代码：`    return (1-damage)**g_power`
- 逐行解释：这一行把当前函数内部已经构造好的结果返回给调用方，使上层流程能够直接拿到局部计算结果。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ R(d) = (1-d)^{g_{\mathrm{power}}} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 299 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“伸长场演化问题与约束求解器构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“伸长场演化问题与约束求解器构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 300 行
- 该行代码：`def Viscous_Relaxation(Lmbda, Lmbda_previous):`
- 逐行解释：Viscous_Relaxation 用条件函数决定是否激活粘性项：只有当前 \Lambda 增大时才加阻尼。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行定义程序结构，本身不进行数值求值。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 301 行
- 该行代码：`    return conditional(Lmbda > Lmbda_previous, 1, 0)`
- 逐行解释：这一行把当前函数内部已经构造好的结果返回给调用方，使上层流程能够直接拿到局部计算结果。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ V(\Lambda,\Lambda_{\mathrm{prev}}) = \begin{cases}1, & \Lambda > \Lambda_{\mathrm{prev}} \\ 0, & \text{otherwise} \end{cases} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 302 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“伸长场演化问题与约束求解器构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“伸长场演化问题与约束求解器构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 303 行
- 该行代码：`dt = 1`
- 逐行解释：这一行执行赋值，把右侧表达式 `1` 的结果保存到变量 `dt` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Delta t = 1 ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 304 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“伸长场演化问题与约束求解器构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“伸长场演化问题与约束求解器构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 305 行
- 该行代码：`Lmbda_WF = dt*(Lmbda*beta)*dx + Relaxation(damage)*dt*(ell**2)*inner(grad(Lmbda), grad(beta))*dx \`
- 逐行解释：这行和下一行组成 \Lambda 方程的弱式，包含质量型项、梯度正则项、驱动项和粘性项。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \int_{\Omega} \Delta t\,\Lambda\,\beta\,\mathrm{d}x + \int_{\Omega} R(d)\,\Delta t\,\ell^2\,\nabla\Lambda\cdot\nabla\beta\,\mathrm{d}x - \int_{\Omega} \Delta t\,\Lambda_{\mathrm{ch}}\,\beta\,\mathrm{d}x ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 306 行
- 该行代码：`        - dt*Lmbda_ch*beta*dx + Viscous_Relaxation(Lmbda, Lmbda_previous)*eta*(Lmbda - Lmbda_previous)*beta*dx`
- 逐行解释：这一行位于“伸长场演化问题与约束求解器构造阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ + \int_{\Omega} V(\Lambda,\Lambda_{\mathrm{prev}})\,\eta\,(\Lambda-\Lambda_{\mathrm{prev}})\,\beta\,\mathrm{d}x ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 307 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“伸长场演化问题与约束求解器构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“伸长场演化问题与约束求解器构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 308 行
- 该行代码：`Lmbda_Jacobian = derivative(Lmbda_WF, Lmbda, Lmbda_trial)`
- 逐行解释：这一行执行赋值，把右侧表达式 `derivative(Lmbda_WF, Lmbda, Lmbda_trial)` 的结果保存到变量 `Lmbda_Jacobian` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ J_{\Lambda} = D F_{\Lambda}(\delta\Lambda) ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

#### 第 309 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“伸长场演化问题与约束求解器构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“伸长场演化问题与约束求解器构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 19 个执行点，解释顺序与源码顺序保持一致。

#### 第 310 行
- 该行代码：`# Lower and upper bound`
- 逐行解释：这是注释行，作用是给“伸长场演化问题与约束求解器构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 20 个执行点，解释顺序与源码顺序保持一致。

#### 第 311 行
- 该行代码：`Lmbda_lb = interpolate(Expression("x[0]>=0 & x[0]<=L/5 & near(x[1], H/2, 0.01*hsize) ? 1.5 : 1", \`
- 逐行解释：下界函数把裂纹尖端附近的一小段区域强制设为 1.5，相当于为伸长变量施加一个初始缺陷/触发条件。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Lambda_{\mathrm{lb}}(x,y) = \begin{cases}1.5, & 0\le x \le L/5\ \text{且}\ y\approx H/2 \\ 1, & \text{其他区域} \end{cases} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 21 个执行点，解释顺序与源码顺序保持一致。

#### 第 312 行
- 该行代码：`                       hsize = hsize, L=L, H=H, degree=0), CG1)`
- 逐行解释：这一行执行赋值，把右侧表达式 `hsize, L=L, H=H, degree=0), CG1)` 的结果保存到变量 `hsize` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ hsize = hsize, L=L, H=H, degree=0), CG1) ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 22 个执行点，解释顺序与源码顺序保持一致。

#### 第 313 行
- 该行代码：`Lmbda_ub = interpolate(Expression("100", degree=0), CG1)`
- 逐行解释：这一行执行赋值，把右侧表达式 `interpolate(Expression("100", degree=0), CG1)` 的结果保存到变量 `Lmbda_ub` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \Lambda_{\mathrm{ub}} = 100 ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 23 个执行点，解释顺序与源码顺序保持一致。

#### 第 314 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“伸长场演化问题与约束求解器构造阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“伸长场演化问题与约束求解器构造阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 24 个执行点，解释顺序与源码顺序保持一致。

#### 第 315 行
- 该行代码：`# Set up the solvers`
- 逐行解释：这是注释行，作用是给“伸长场演化问题与约束求解器构造阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 25 个执行点，解释顺序与源码顺序保持一致。

#### 第 316 行
- 该行代码：`problem_Lmbda = NonlinearVariationalProblem(Lmbda_WF, Lmbda, bc_Lmbda, J=Lmbda_Jacobian)`
- 逐行解释：这一行在 FEniCS 中实例化问题对象或求解器对象，把前面定义的弱式、边界条件和 Jacobian 绑定起来。
- 运行影响：执行后，脚本就具备了真正数值求解所需的对象封装；后面的 `.solve()` 会直接调用这里创建的实例。
- 公式/符号：[ problem_Lmbda = NonlinearVariationalProblem(Lmbda_WF, Lmbda, bc_Lmbda, J=Lmbda_Jacobian) ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 26 个执行点，解释顺序与源码顺序保持一致。

#### 第 317 行
- 该行代码：`problem_Lmbda.set_bounds(Lmbda_lb, Lmbda_ub)`
- 逐行解释：这一行在 FEniCS 中实例化问题对象或求解器对象，把前面定义的弱式、边界条件和 Jacobian 绑定起来。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 27 个执行点，解释顺序与源码顺序保持一致。

#### 第 318 行
- 该行代码：`solver_Lmbda = NonlinearVariationalSolver(problem_Lmbda)`
- 逐行解释：这一行在 FEniCS 中实例化问题对象或求解器对象，把前面定义的弱式、边界条件和 Jacobian 绑定起来。
- 运行影响：执行后，脚本就具备了真正数值求解所需的对象封装；后面的 `.solve()` 会直接调用这里创建的实例。
- 公式/符号：[ solver_Lmbda = NonlinearVariationalSolver(problem_Lmbda) ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 28 个执行点，解释顺序与源码顺序保持一致。

#### 第 319 行
- 该行代码：`solver_Lmbda.parameters.update(solver_Lmbda_parameters)`
- 逐行解释：这一行在 FEniCS 中实例化问题对象或求解器对象，把前面定义的弱式、边界条件和 Jacobian 绑定起来。
- 运行影响：这行执行后会在“伸长场演化问题与约束求解器构造阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“伸长场演化问题与约束求解器构造阶段”内部的第 29 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 294 行对应公式：[ \Lambda_0(\mathbf{x}) = 1 ]
- 第 295 行对应公式：[ \Lambda_{\mathrm{prev}}(\mathbf{x}) = 1 ]
- 第 298 行对应公式：[ R(d) = (1-d)^{g_{\mathrm{power}}} ]
- 第 301 行对应公式：[ V(\Lambda,\Lambda_{\mathrm{prev}}) = \begin{cases}1, & \Lambda > \Lambda_{\mathrm{prev}} \\ 0, & \text{otherwise} \end{cases} ]
- 第 303 行对应公式：[ \Delta t = 1 ]
- 第 305 行对应公式：[ \int_{\Omega} \Delta t\,\Lambda\,\beta\,\mathrm{d}x + \int_{\Omega} R(d)\,\Delta t\,\ell^2\,\nabla\Lambda\cdot\nabla\beta\,\mathrm{d}x - \int_{\Omega} \Delta t\,\Lambda_{\mathrm{ch}}\,\beta\,\mathrm{d}x ]
- 第 306 行对应公式：[ + \int_{\Omega} V(\Lambda,\Lambda_{\mathrm{prev}})\,\eta\,(\Lambda-\Lambda_{\mathrm{prev}})\,\beta\,\mathrm{d}x ]
- 第 308 行对应公式：[ J_{\Lambda} = D F_{\Lambda}(\delta\Lambda) ]
- 第 311 行对应公式：[ \Lambda_{\mathrm{lb}}(x,y) = \begin{cases}1.5, & 0\le x \le L/5\ \text{且}\ y\approx H/2 \\ 1, & \text{其他区域} \end{cases} ]
- 第 312 行对应公式：[ hsize = hsize, L=L, H=H, degree=0), CG1) ]

## 分段 14：第 320-341 行 —— 加载序列与结果容器初始化阶段

- 本段定位：创建载荷步数组、能量与迭代容器、XDMF 总输出文件以及若干后处理列表。
- 与前后关系：本段结束后会进入“主加载循环与交替最小化迭代阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
320: 
321: 
322: load_multipliers = np.linspace(load_min, load_max, load_steps)
323: print("load:", load_multipliers)
324: 
325: # initialization of vectors to store data of interest
326: energies   = np.zeros((len(load_multipliers), 5))
327: iterations = np.zeros((len(load_multipliers), 2))
328: 
329: # Split solutions
330: (u, p) = w_p.split()
331: # Data file name
332: file_tot = XDMFFile(MPI.comm_world, savedir + "/results.xdmf")
333: # Saves the file in case of interruption
334: file_tot.parameters["rewrite_function_mesh"] = False
335: file_tot.parameters["functions_share_mesh"]  = True
336: file_tot.parameters["flush_output"]          = True
337: 
338: J_integral_list = []
339: crack_length = []
340: traction_x_list = []
341: traction_y_list = []
```

### 分段内逐行解释

#### 第 320 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 321 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 322 行
- 该行代码：`load_multipliers = np.linspace(load_min, load_max, load_steps)`
- 逐行解释：载荷步序列一次性生成好，主循环将按这个数组从 0 逐渐推进到 0.6。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ t_i = t_{\min} + \frac{i}{N_{\mathrm{load}}-1}(t_{\max}-t_{\min}) ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 323 行
- 该行代码：`print("load:", load_multipliers)`
- 逐行解释：这一行把关键中间结果打印到终端，便于监控求解进度、收敛性和后处理数据。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 324 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“加载序列与结果容器初始化阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“加载序列与结果容器初始化阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 325 行
- 该行代码：`# initialization of vectors to store data of interest`
- 逐行解释：这是注释行，作用是给“加载序列与结果容器初始化阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 326 行
- 该行代码：`energies   = np.zeros((len(load_multipliers), 5))`
- 逐行解释：这一行执行赋值，把右侧表达式 `np.zeros((len(load_multipliers), 5))` 的结果保存到变量 `energies` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathrm{energies} \in \mathbb{R}^{N_{\mathrm{load}}\times 5} ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 327 行
- 该行代码：`iterations = np.zeros((len(load_multipliers), 2))`
- 逐行解释：这一行执行赋值，把右侧表达式 `np.zeros((len(load_multipliers), 2))` 的结果保存到变量 `iterations` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathrm{iterations} \in \mathbb{R}^{N_{\mathrm{load}}\times 2} ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 328 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“加载序列与结果容器初始化阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“加载序列与结果容器初始化阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 329 行
- 该行代码：`# Split solutions`
- 逐行解释：这是注释行，作用是给“加载序列与结果容器初始化阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 330 行
- 该行代码：`(u, p) = w_p.split()`
- 逐行解释：在真正进入时间循环前先把混合解拆成位移和压力，方便后续单独命名与输出。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ (u, p) = w_p.split() ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 331 行
- 该行代码：`# Data file name`
- 逐行解释：这是注释行，作用是给“加载序列与结果容器初始化阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 332 行
- 该行代码：`file_tot = XDMFFile(MPI.comm_world, savedir + "/results.xdmf")`
- 逐行解释：这一行执行赋值，把右侧表达式 `XDMFFile(MPI.comm_world, savedir + "/results.xdmf")` 的结果保存到变量 `file_tot` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ file_tot = XDMFFile(MPI.comm_world, savedir + "/results.xdmf") ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 333 行
- 该行代码：`# Saves the file in case of interruption`
- 逐行解释：这是注释行，作用是给“加载序列与结果容器初始化阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 334 行
- 该行代码：`file_tot.parameters["rewrite_function_mesh"] = False`
- 逐行解释：这一行执行赋值，把右侧表达式 `False` 的结果保存到变量 `file_tot.parameters["rewrite_function_mesh"]` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ file_tot.parameters["rewrite_function_mesh"] = False ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 335 行
- 该行代码：`file_tot.parameters["functions_share_mesh"]  = True`
- 逐行解释：这一行执行赋值，把右侧表达式 `True` 的结果保存到变量 `file_tot.parameters["functions_share_mesh"]` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ file_tot.parameters["functions_share_mesh"] = True ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 336 行
- 该行代码：`file_tot.parameters["flush_output"]          = True`
- 逐行解释：这一行执行赋值，把右侧表达式 `True` 的结果保存到变量 `file_tot.parameters["flush_output"]` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ file_tot.parameters["flush_output"] = True ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 337 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“加载序列与结果容器初始化阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“加载序列与结果容器初始化阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

#### 第 338 行
- 该行代码：`J_integral_list = []`
- 逐行解释：这一行执行赋值，把右侧表达式 `[]` 的结果保存到变量 `J_integral_list` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ J_integral_list = [] ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 19 个执行点，解释顺序与源码顺序保持一致。

#### 第 339 行
- 该行代码：`crack_length = []`
- 逐行解释：这一行执行赋值，把右侧表达式 `[]` 的结果保存到变量 `crack_length` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ crack_length = [] ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 20 个执行点，解释顺序与源码顺序保持一致。

#### 第 340 行
- 该行代码：`traction_x_list = []`
- 逐行解释：这一行执行赋值，把右侧表达式 `[]` 的结果保存到变量 `traction_x_list` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ traction_x_list = [] ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 21 个执行点，解释顺序与源码顺序保持一致。

#### 第 341 行
- 该行代码：`traction_y_list = []`
- 逐行解释：这一行执行赋值，把右侧表达式 `[]` 的结果保存到变量 `traction_y_list` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“加载序列与结果容器初始化阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ traction_y_list = [] ]
- 行内顺序关系：这一行属于“加载序列与结果容器初始化阶段”内部的第 22 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 322 行对应公式：[ t_i = t_{\min} + \frac{i}{N_{\mathrm{load}}-1}(t_{\max}-t_{\min}) ]
- 第 326 行对应公式：[ \mathrm{energies} \in \mathbb{R}^{N_{\mathrm{load}}\times 5} ]
- 第 327 行对应公式：[ \mathrm{iterations} \in \mathbb{R}^{N_{\mathrm{load}}\times 2} ]
- 第 330 行对应公式：[ (u, p) = w_p.split() ]
- 第 332 行对应公式：[ file_tot = XDMFFile(MPI.comm_world, savedir + "/results.xdmf") ]
- 第 334 行对应公式：[ file_tot.parameters["rewrite_function_mesh"] = False ]
- 第 335 行对应公式：[ file_tot.parameters["functions_share_mesh"] = True ]
- 第 336 行对应公式：[ file_tot.parameters["flush_output"] = True ]
- 第 338 行对应公式：[ J_integral_list = [] ]
- 第 339 行对应公式：[ crack_length = [] ]

## 分段 15：第 342-382 行 —— 主加载循环与交替最小化迭代阶段

- 本段定位：外层按载荷步推进，内层在固定损伤与固定位移之间交替求解，直到 \Lambda 收敛。
- 与前后关系：本段结束后会进入“状态更新、投影输出与派生量写出阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
342: # ----------------------------------------------------------------------------
343: for (i_t, t) in enumerate(load_multipliers):
344:     # Structure used for one printout of the statement
345:     if MPI.rank(MPI.comm_world) == 0:
346:         print("\033[1;32m--- Starting of Time step {0:2d}: t = {1:4f} ---\033[1;m".format(i_t, t))
347: 
348:     # Alternate Mininimization Scheme
349:     # -------------------------------------------------------------------------
350:     # Solve for u holding alpha constant then solve for alpha holding u constant
351:     iteration = 1           # Initialization of iteration loop
352:     err_Lmbda = 1
353: 
354:     # Conditions for iteration
355:     while err_Lmbda > AM_tolerance and iteration < maxiteration:
356:         # solve elastic problem
357:         solver_u.solve()
358:         # solve damage problem with box constraint
359:         solver_Lmbda.solve()
360:         
361:         Lmbda_numeric = project(Lmbda, CG1)
362:         damage_values = np.zeros_like(Lmbda_numeric.vector().get_local())
363:         for i, value in enumerate(Lmbda_numeric.vector().get_local()):
364:             if value < lambda_i:
365:                 # print(f"Index: {i}, Value: {value}")
366:                 new_damage = 0
367:                 damage_values[i] = max(new_damage, damage_previous.vector().get_local()[i])
368:             else:
369:                 new_damage = 1 - ((lambda_i-1)/(value-1)) * (1 - alpha_damage + alpha_damage * np.exp(-beta_damage * (value - lambda_i)))
370:                 damage_values[i] = max(new_damage, damage_previous.vector().get_local()[i])  # Ensure new damage doesn't go below previous damage
371:                 # print(f"YYYYYYYYYYYYYYYYYYYYYYYYIndex: {i}, Value: {value}, and damage is {damage_values[i]}")
372:         damage.vector().set_local(damage_values)
373:                 
374:         Lmbda_error = Lmbda.vector() - Lmbda_0.vector()
375:         err_Lmbda = Lmbda_error.norm('linf')
376:         print ("AM Iteration: {0:3d},  Lmbda_error: {1:>14.8f}".format(iteration, err_Lmbda))
377:         Lmbda_0.assign(Lmbda)
378: 
379: 
380:         volume_ratio = assemble(J/(L*H)*dx)
381:         iteration = iteration + 1
382: 
```

### 分段内逐行解释

#### 第 342 行
- 该行代码：`# ----------------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“当前阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 343 行
- 该行代码：`for (i_t, t) in enumerate(load_multipliers):`
- 逐行解释：从这里开始进入整个脚本最核心的运行阶段：外层是载荷步循环，内层是交替最小化迭代。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行对应算法迭代或遍历结构，数学上可理解为重复执行同一离散更新。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 344 行
- 该行代码：`    # Structure used for one printout of the statement`
- 逐行解释：这是注释行，作用是给“主加载循环与交替最小化迭代阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 345 行
- 该行代码：`    if MPI.rank(MPI.comm_world) == 0:`
- 逐行解释：这一行开始一个条件分支，表示程序会根据当前变量状态选择不同路径；这通常意味着模型里存在阈值、边界保护或输出控制。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行体现条件逻辑，而非新的数学算子定义。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 346 行
- 该行代码：`        print("\033[1;32m--- Starting of Time step {0:2d}: t = {1:4f} ---\033[1;m".format(i_t, t))`
- 逐行解释：这一行把关键中间结果打印到终端，便于监控求解进度、收敛性和后处理数据。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{ANSI 彩色打印会输出当前载荷步编号 } i_t \text{ 与载荷值 } t ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 347 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“主加载循环与交替最小化迭代阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“主加载循环与交替最小化迭代阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 348 行
- 该行代码：`    # Alternate Mininimization Scheme`
- 逐行解释：这是注释行，作用是给“主加载循环与交替最小化迭代阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 349 行
- 该行代码：`    # -------------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“主加载循环与交替最小化迭代阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 350 行
- 该行代码：`    # Solve for u holding alpha constant then solve for alpha holding u constant`
- 逐行解释：这是注释行，作用是给“主加载循环与交替最小化迭代阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 351 行
- 该行代码：`    iteration = 1           # Initialization of iteration loop`
- 逐行解释：这一行执行赋值，把右侧表达式 `1           # Initialization of iteration loop` 的结果保存到变量 `iteration` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ iteration = 1           # Initialization of iteration loop ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 352 行
- 该行代码：`    err_Lmbda = 1`
- 逐行解释：这一行执行赋值，把右侧表达式 `1` 的结果保存到变量 `err_Lmbda` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ err_Lmbda = 1 ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 353 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“主加载循环与交替最小化迭代阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“主加载循环与交替最小化迭代阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 354 行
- 该行代码：`    # Conditions for iteration`
- 逐行解释：这是注释行，作用是给“主加载循环与交替最小化迭代阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 355 行
- 该行代码：`    while err_Lmbda > AM_tolerance and iteration < maxiteration:`
- 逐行解释：交替最小化的停止准则同时看误差和最大迭代次数，避免误差迟迟不降时死循环。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{while } \|\Lambda-\Lambda_0\|_{\infty} > \varepsilon_{\mathrm{AM}} \text{ and } k < N_{\mathrm{AM}}^{\max} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 356 行
- 该行代码：`        # solve elastic problem`
- 逐行解释：这是注释行，作用是给“主加载循环与交替最小化迭代阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 357 行
- 该行代码：`        solver_u.solve()`
- 逐行解释：先在固定损伤下求位移/压力，再在固定位移下求 \Lambda，这就是交替最小化的基本思路。
- 运行影响：这一行会触发一次非线性求解，是主流程中真正耗时、也最直接改变场变量数值的步骤。
- 公式/符号：[ \text{本行执行非线性方程组求解：残量 } = 0 ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 358 行
- 该行代码：`        # solve damage problem with box constraint`
- 逐行解释：这是注释行，作用是给“主加载循环与交替最小化迭代阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 359 行
- 该行代码：`        solver_Lmbda.solve()`
- 逐行解释：这一行在 FEniCS 中实例化问题对象或求解器对象，把前面定义的弱式、边界条件和 Jacobian 绑定起来。
- 运行影响：这一行会触发一次非线性求解，是主流程中真正耗时、也最直接改变场变量数值的步骤。
- 公式/符号：[ \text{本行执行非线性方程组求解：残量 } = 0 ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

#### 第 360 行
- 该行代码：`        `
- 逐行解释：这是一个空行，用来在“主加载循环与交替最小化迭代阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“主加载循环与交替最小化迭代阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 19 个执行点，解释顺序与源码顺序保持一致。

#### 第 361 行
- 该行代码：`        Lmbda_numeric = project(Lmbda, CG1)`
- 逐行解释：将连续场 \Lambda 投影到 CG1 便于逐节点读取数值，然后用 NumPy 手工更新损伤变量。
- 运行影响：这一行会生成一个离散投影结果，主要服务于输出、后处理或逐点读取。
- 公式/符号：[ Lmbda_numeric = project(Lmbda, CG1) ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 20 个执行点，解释顺序与源码顺序保持一致。

#### 第 362 行
- 该行代码：`        damage_values = np.zeros_like(Lmbda_numeric.vector().get_local())`
- 逐行解释：这一行执行赋值，把右侧表达式 `np.zeros_like(Lmbda_numeric.vector().get_local())` 的结果保存到变量 `damage_values` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ damage_values = np.zeros_like(Lmbda_numeric.vector().get_local()) ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 21 个执行点，解释顺序与源码顺序保持一致。

#### 第 363 行
- 该行代码：`        for i, value in enumerate(Lmbda_numeric.vector().get_local()):`
- 逐行解释：这一行开启循环，说明程序要对一组载荷步、顶点或向量分量重复执行同一类操作。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行对应算法迭代或遍历结构，数学上可理解为重复执行同一离散更新。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 22 个执行点，解释顺序与源码顺序保持一致。

#### 第 364 行
- 该行代码：`            if value < lambda_i:`
- 逐行解释：这一行开始一个条件分支，表示程序会根据当前变量状态选择不同路径；这通常意味着模型里存在阈值、边界保护或输出控制。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行体现条件逻辑，而非新的数学算子定义。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 23 个执行点，解释顺序与源码顺序保持一致。

#### 第 365 行
- 该行代码：`                # print(f"Index: {i}, Value: {value}")`
- 逐行解释：这是注释行，作用是给“主加载循环与交替最小化迭代阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 24 个执行点，解释顺序与源码顺序保持一致。

#### 第 366 行
- 该行代码：`                new_damage = 0`
- 逐行解释：这一行执行赋值，把右侧表达式 `0` 的结果保存到变量 `new_damage` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ new_damage = 0 ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 25 个执行点，解释顺序与源码顺序保持一致。

#### 第 367 行
- 该行代码：`                damage_values[i] = max(new_damage, damage_previous.vector().get_local()[i])`
- 逐行解释：这一行执行赋值，把右侧表达式 `max(new_damage, damage_previous.vector().get_local()[i])` 的结果保存到变量 `damage_values[i]` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ damage_values[i] = max(new_damage, damage_previous.vector().get_local()[i]) ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 26 个执行点，解释顺序与源码顺序保持一致。

#### 第 368 行
- 该行代码：`            else:`
- 逐行解释：这一行表示前述条件均不满足时的兜底路径，用来保证流程完整。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行体现条件逻辑，而非新的数学算子定义。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 27 个执行点，解释顺序与源码顺序保持一致。

#### 第 369 行
- 该行代码：`                new_damage = 1 - ((lambda_i-1)/(value-1)) * (1 - alpha_damage + alpha_damage * np.exp(-beta_damage * (value - lambda_i)))`
- 逐行解释：这里是真正的损伤演化公式：当伸长超过阈值后，损伤按照指数软化规律增长。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ d_{\mathrm{new}} = 1 - \frac{\lambda_i-1}{\Lambda-1}\left(1-\alpha_{\mathrm{damage}} + \alpha_{\mathrm{damage}} e^{-\beta_{\mathrm{damage}}(\Lambda-\lambda_i)}\right) ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 28 个执行点，解释顺序与源码顺序保持一致。

#### 第 370 行
- 该行代码：`                damage_values[i] = max(new_damage, damage_previous.vector().get_local()[i])  # Ensure new damage doesn't go below previous damage`
- 逐行解释：通过与 `damage_previous` 取最大值强制损伤不可逆，即裂纹只能扩展，不能自愈。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ d \leftarrow \max(d_{\mathrm{new}}, d_{\mathrm{prev}}) ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 29 个执行点，解释顺序与源码顺序保持一致。

#### 第 371 行
- 该行代码：`                # print(f"YYYYYYYYYYYYYYYYYYYYYYYYIndex: {i}, Value: {value}, and damage is {damage_values[i]}")`
- 逐行解释：这是注释行，作用是给“主加载循环与交替最小化迭代阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 30 个执行点，解释顺序与源码顺序保持一致。

#### 第 372 行
- 该行代码：`        damage.vector().set_local(damage_values)`
- 逐行解释：这一行位于“主加载循环与交替最小化迭代阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这一行会把新状态写回有限元函数对象，因此会实质性改变后续计算使用的历史变量或当前场。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 31 个执行点，解释顺序与源码顺序保持一致。

#### 第 373 行
- 该行代码：`                `
- 逐行解释：这是一个空行，用来在“主加载循环与交替最小化迭代阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“主加载循环与交替最小化迭代阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 32 个执行点，解释顺序与源码顺序保持一致。

#### 第 374 行
- 该行代码：`        Lmbda_error = Lmbda.vector() - Lmbda_0.vector()`
- 逐行解释：误差向量由当前 \Lambda 与上一次迭代的 \Lambda_0 相减得到，它衡量交替最小化是否收敛。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathbf{e}_{\Lambda} = \Lambda - \Lambda_0 ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 33 个执行点，解释顺序与源码顺序保持一致。

#### 第 375 行
- 该行代码：`        err_Lmbda = Lmbda_error.norm('linf')`
- 逐行解释：这一行执行赋值，把右侧表达式 `Lmbda_error.norm('linf')` 的结果保存到变量 `err_Lmbda` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathrm{err}_{\Lambda} = \|\mathbf{e}_{\Lambda}\|_{\infty} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 34 个执行点，解释顺序与源码顺序保持一致。

#### 第 376 行
- 该行代码：`        print ("AM Iteration: {0:3d},  Lmbda_error: {1:>14.8f}".format(iteration, err_Lmbda))`
- 逐行解释：这一行位于“主加载循环与交替最小化迭代阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 35 个执行点，解释顺序与源码顺序保持一致。

#### 第 377 行
- 该行代码：`        Lmbda_0.assign(Lmbda)`
- 逐行解释：这一行位于“主加载循环与交替最小化迭代阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这一行会把新状态写回有限元函数对象，因此会实质性改变后续计算使用的历史变量或当前场。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 36 个执行点，解释顺序与源码顺序保持一致。

#### 第 378 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“主加载循环与交替最小化迭代阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“主加载循环与交替最小化迭代阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 37 个执行点，解释顺序与源码顺序保持一致。

#### 第 379 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“主加载循环与交替最小化迭代阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“主加载循环与交替最小化迭代阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 38 个执行点，解释顺序与源码顺序保持一致。

#### 第 380 行
- 该行代码：`        volume_ratio = assemble(J/(L*H)*dx)`
- 逐行解释：虽然 `volume_ratio` 后面没有继续使用，但它提供了一个监控不可压缩性是否保持的诊断量。
- 运行影响：这一行会把弱式中的积分真正计算成标量，是监测物理量或后处理指标的关键步骤。
- 公式/符号：[ \bar{J} = \frac{1}{L\,H}\int_{\Omega} J\,\mathrm{d}x ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 39 个执行点，解释顺序与源码顺序保持一致。

#### 第 381 行
- 该行代码：`        iteration = iteration + 1`
- 逐行解释：这一行执行赋值，把右侧表达式 `iteration + 1` 的结果保存到变量 `iteration` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“主加载循环与交替最小化迭代阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ iteration = iteration + 1 ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 40 个执行点，解释顺序与源码顺序保持一致。

#### 第 382 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“主加载循环与交替最小化迭代阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“主加载循环与交替最小化迭代阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“主加载循环与交替最小化迭代阶段”内部的第 41 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 346 行对应公式：[ \text{ANSI 彩色打印会输出当前载荷步编号 } i_t \text{ 与载荷值 } t ]
- 第 351 行对应公式：[ iteration = 1           # Initialization of iteration loop ]
- 第 352 行对应公式：[ err_Lmbda = 1 ]
- 第 355 行对应公式：[ \text{while } \|\Lambda-\Lambda_0\|_{\infty} > \varepsilon_{\mathrm{AM}} \text{ and } k < N_{\mathrm{AM}}^{\max} ]
- 第 361 行对应公式：[ Lmbda_numeric = project(Lmbda, CG1) ]
- 第 362 行对应公式：[ damage_values = np.zeros_like(Lmbda_numeric.vector().get_local()) ]
- 第 366 行对应公式：[ new_damage = 0 ]
- 第 367 行对应公式：[ damage_values[i] = max(new_damage, damage_previous.vector().get_local()[i]) ]
- 第 369 行对应公式：[ d_{\mathrm{new}} = 1 - \frac{\lambda_i-1}{\Lambda-1}\left(1-\alpha_{\mathrm{damage}} + \alpha_{\mathrm{damage}} e^{-\beta_{\mathrm{damage}}(\Lambda-\lambda_i)}\right) ]
- 第 370 行对应公式：[ d \leftarrow \max(d_{\mathrm{new}}, d_{\mathrm{prev}}) ]

## 分段 16：第 383-418 行 —— 状态更新、投影输出与派生量写出阶段

- 本段定位：更新历史变量，把主变量和派生量投影并写出，为后处理和可视化准备完整结果集。
- 与前后关系：本段结束后会进入“边界位移更新与后处理阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
383: 
384:     # updating the lower bound to account for the irreversibility
385:     Lmbda_previous.assign(Lmbda)
386:     damage_previous.assign(damage)
387: 
388:     # Project
389:     local_project(P(u, damage), T_DG0, PTensor)
390:     local_project(F, T_DG0, FTensor)
391:     local_project(J, CG1, JScalar)
392: 
393:     # Rename for paraview
394:     u.rename("Displacement", "u")
395:     p.rename("Pressure", "p")
396:     Lmbda.rename("Stretch", "Lmbda")
397:     damage.rename("Damage", "damage")
398: 
399:     # Write solution to file
400:     Lmbda_ch_projected = project(Lmbda_ch, CG1)
401:     Lmbda_ch_projected.rename("Lambda_ch", "Lmbda_ch_projected")
402:     file_tot.write(u, t)
403:     file_tot.write(p, t)
404:     file_tot.write(Lmbda, t)
405:     file_tot.write(Lmbda_ch_projected, t)
406:     file_tot.write(damage, t)
407:     file_tot.write(PTensor, t)
408:     file_tot.write(FTensor, t)
409:     file_tot.write(JScalar,t)
410: 
411:     # Define the expression for the gradient term
412:     Vct_space = VectorFunctionSpace(mesh, "Lagrange", 1)
413:     laplacian_Lmbda = grad(Lmbda)
414:     grad_term = (ell**2) * laplacian_Lmbda
415:     grad_term_proj = project(grad_term, Vct_space)
416:     grad_term_proj.rename("Grad_Lambda_Term", "grad_term_proj")
417:     file_tot.write(grad_term_proj,t)
418: 
```

### 分段内逐行解释

#### 第 383 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 384 行
- 该行代码：`    # updating the lower bound to account for the irreversibility`
- 逐行解释：这是注释行，作用是给“状态更新、投影输出与派生量写出阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 385 行
- 该行代码：`    Lmbda_previous.assign(Lmbda)`
- 逐行解释：外层载荷步结束后，把当前 \Lambda 保存为“上一时刻”状态，供下一载荷步的粘性项与不可逆约束使用。
- 运行影响：这一行会把新状态写回有限元函数对象，因此会实质性改变后续计算使用的历史变量或当前场。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 386 行
- 该行代码：`    damage_previous.assign(damage)`
- 逐行解释：这一行位于“状态更新、投影输出与派生量写出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这一行会把新状态写回有限元函数对象，因此会实质性改变后续计算使用的历史变量或当前场。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 387 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“状态更新、投影输出与派生量写出阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“状态更新、投影输出与派生量写出阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 388 行
- 该行代码：`    # Project`
- 逐行解释：这是注释行，作用是给“状态更新、投影输出与派生量写出阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 389 行
- 该行代码：`    local_project(P(u, damage), T_DG0, PTensor)`
- 逐行解释：现在开始后处理投影：先把应力投影出来，随后还会投影 F 和 J，便于可视化软件直接读取。
- 运行影响：这一行会生成一个离散投影结果，主要服务于输出、后处理或逐点读取。
- 公式/符号：[ \mathbf{P}_h = \Pi_{DG0}\,\mathbf{P}(\mathbf{u},d) ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 390 行
- 该行代码：`    local_project(F, T_DG0, FTensor)`
- 逐行解释：这一行位于“状态更新、投影输出与派生量写出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这一行会生成一个离散投影结果，主要服务于输出、后处理或逐点读取。
- 公式/符号：[ \mathbf{F}_h = \Pi_{DG0}\,\mathbf{F} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 391 行
- 该行代码：`    local_project(J, CG1, JScalar)`
- 逐行解释：这一行位于“状态更新、投影输出与派生量写出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这一行会生成一个离散投影结果，主要服务于输出、后处理或逐点读取。
- 公式/符号：[ J_h = \Pi_{CG1}\,J ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 392 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“状态更新、投影输出与派生量写出阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“状态更新、投影输出与派生量写出阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 393 行
- 该行代码：`    # Rename for paraview`
- 逐行解释：这是注释行，作用是给“状态更新、投影输出与派生量写出阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 394 行
- 该行代码：`    u.rename("Displacement", "u")`
- 逐行解释：这一行位于“状态更新、投影输出与派生量写出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 395 行
- 该行代码：`    p.rename("Pressure", "p")`
- 逐行解释：这一行位于“状态更新、投影输出与派生量写出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 396 行
- 该行代码：`    Lmbda.rename("Stretch", "Lmbda")`
- 逐行解释：这一行位于“状态更新、投影输出与派生量写出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 397 行
- 该行代码：`    damage.rename("Damage", "damage")`
- 逐行解释：这一行位于“状态更新、投影输出与派生量写出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 398 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“状态更新、投影输出与派生量写出阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“状态更新、投影输出与派生量写出阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 399 行
- 该行代码：`    # Write solution to file`
- 逐行解释：这是注释行，作用是给“状态更新、投影输出与派生量写出阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 400 行
- 该行代码：`    Lmbda_ch_projected = project(Lmbda_ch, CG1)`
- 逐行解释：链伸长上限处理后的 `Lmbda_ch` 也被投影保存，这样可以直接在 ParaView 中看驱动损伤的局部伸长分布。
- 运行影响：这一行会生成一个离散投影结果，主要服务于输出、后处理或逐点读取。
- 公式/符号：[ \Lambda_{\mathrm{ch},h} = \Pi_{CG1}\,\Lambda_{\mathrm{ch}} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

#### 第 401 行
- 该行代码：`    Lmbda_ch_projected.rename("Lambda_ch", "Lmbda_ch_projected")`
- 逐行解释：这一行位于“状态更新、投影输出与派生量写出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 19 个执行点，解释顺序与源码顺序保持一致。

#### 第 402 行
- 该行代码：`    file_tot.write(u, t)`
- 逐行解释：这一行把当前网格函数或解场写入可视化文件，便于在 ParaView 等软件中按时间步查看结果。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 20 个执行点，解释顺序与源码顺序保持一致。

#### 第 403 行
- 该行代码：`    file_tot.write(p, t)`
- 逐行解释：这一行把当前网格函数或解场写入可视化文件，便于在 ParaView 等软件中按时间步查看结果。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 21 个执行点，解释顺序与源码顺序保持一致。

#### 第 404 行
- 该行代码：`    file_tot.write(Lmbda, t)`
- 逐行解释：这一行把当前网格函数或解场写入可视化文件，便于在 ParaView 等软件中按时间步查看结果。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 22 个执行点，解释顺序与源码顺序保持一致。

#### 第 405 行
- 该行代码：`    file_tot.write(Lmbda_ch_projected, t)`
- 逐行解释：这一行把当前网格函数或解场写入可视化文件，便于在 ParaView 等软件中按时间步查看结果。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 23 个执行点，解释顺序与源码顺序保持一致。

#### 第 406 行
- 该行代码：`    file_tot.write(damage, t)`
- 逐行解释：这一行把当前网格函数或解场写入可视化文件，便于在 ParaView 等软件中按时间步查看结果。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 24 个执行点，解释顺序与源码顺序保持一致。

#### 第 407 行
- 该行代码：`    file_tot.write(PTensor, t)`
- 逐行解释：这一行把当前网格函数或解场写入可视化文件，便于在 ParaView 等软件中按时间步查看结果。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 25 个执行点，解释顺序与源码顺序保持一致。

#### 第 408 行
- 该行代码：`    file_tot.write(FTensor, t)`
- 逐行解释：这一行把当前网格函数或解场写入可视化文件，便于在 ParaView 等软件中按时间步查看结果。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 26 个执行点，解释顺序与源码顺序保持一致。

#### 第 409 行
- 该行代码：`    file_tot.write(JScalar,t)`
- 逐行解释：这一行把当前网格函数或解场写入可视化文件，便于在 ParaView 等软件中按时间步查看结果。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 27 个执行点，解释顺序与源码顺序保持一致。

#### 第 410 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“状态更新、投影输出与派生量写出阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“状态更新、投影输出与派生量写出阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 28 个执行点，解释顺序与源码顺序保持一致。

#### 第 411 行
- 该行代码：`    # Define the expression for the gradient term`
- 逐行解释：这是注释行，作用是给“状态更新、投影输出与派生量写出阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 29 个执行点，解释顺序与源码顺序保持一致。

#### 第 412 行
- 该行代码：`    Vct_space = VectorFunctionSpace(mesh, "Lagrange", 1)`
- 逐行解释：这一行执行赋值，把右侧表达式 `VectorFunctionSpace(mesh, "Lagrange", 1)` 的结果保存到变量 `Vct_space` 中；后续主流程会直接引用这个量。
- 运行影响：这一行一旦执行，就确定了某类未知量或投影量的离散空间，从而决定后续向量长度、插值阶次和输出精度。
- 公式/符号：[ Vct_space = VectorFunctionSpace(mesh, "Lagrange", 1) ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 30 个执行点，解释顺序与源码顺序保持一致。

#### 第 413 行
- 该行代码：`    laplacian_Lmbda = grad(Lmbda)`
- 逐行解释：这里虽然变量名写成 `laplacian_Lmbda`，但实际计算的是梯度 `grad(Lmbda)`，不是拉普拉斯算子。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \nabla \Lambda ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 31 个执行点，解释顺序与源码顺序保持一致。

#### 第 414 行
- 该行代码：`    grad_term = (ell**2) * laplacian_Lmbda`
- 逐行解释：这一行执行赋值，把右侧表达式 `(ell**2) * laplacian_Lmbda` 的结果保存到变量 `grad_term` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \ell^2\nabla \Lambda ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 32 个执行点，解释顺序与源码顺序保持一致。

#### 第 415 行
- 该行代码：`    grad_term_proj = project(grad_term, Vct_space)`
- 逐行解释：这一行执行赋值，把右侧表达式 `project(grad_term, Vct_space)` 的结果保存到变量 `grad_term_proj` 中；后续主流程会直接引用这个量。
- 运行影响：这一行会生成一个离散投影结果，主要服务于输出、后处理或逐点读取。
- 公式/符号：[ (\ell^2\nabla\Lambda)_h = \Pi_{V_{CG1}}(\ell^2\nabla\Lambda) ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 33 个执行点，解释顺序与源码顺序保持一致。

#### 第 416 行
- 该行代码：`    grad_term_proj.rename("Grad_Lambda_Term", "grad_term_proj")`
- 逐行解释：这一行位于“状态更新、投影输出与派生量写出阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 34 个执行点，解释顺序与源码顺序保持一致。

#### 第 417 行
- 该行代码：`    file_tot.write(grad_term_proj,t)`
- 逐行解释：这一行把当前网格函数或解场写入可视化文件，便于在 ParaView 等软件中按时间步查看结果。
- 运行影响：这行执行后会在“状态更新、投影输出与派生量写出阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 35 个执行点，解释顺序与源码顺序保持一致。

#### 第 418 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“状态更新、投影输出与派生量写出阶段”内部的第 36 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 389 行对应公式：[ \mathbf{P}_h = \Pi_{DG0}\,\mathbf{P}(\mathbf{u},d) ]
- 第 390 行对应公式：[ \mathbf{F}_h = \Pi_{DG0}\,\mathbf{F} ]
- 第 391 行对应公式：[ J_h = \Pi_{CG1}\,J ]
- 第 400 行对应公式：[ \Lambda_{\mathrm{ch},h} = \Pi_{CG1}\,\Lambda_{\mathrm{ch}} ]
- 第 412 行对应公式：[ Vct_space = VectorFunctionSpace(mesh, "Lagrange", 1) ]
- 第 413 行对应公式：[ \nabla \Lambda ]
- 第 414 行对应公式：[ \ell^2\nabla \Lambda ]
- 第 415 行对应公式：[ (\ell^2\nabla\Lambda)_h = \Pi_{V_{CG1}}(\ell^2\nabla\Lambda) ]

## 分段 17：第 419-455 行 —— 边界位移更新与后处理阶段

- 本段定位：更新下一步边界位移参数，并计算裂纹长度、J 积分和边界反力。
- 与前后关系：本段结束后会进入“结果保存与绘图阶段”。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
419:     # Update the displacement with each iteration
420:     u1.t = t
421:     u2.t = t
422: 
423:     # Post-processing
424:     # ----------------------------------------
425:     damage_values = damage.compute_vertex_values(mesh)
426:     # Find the rightmost node with damage >= 0.9
427:     rightmost_node = None
428:     rightmost_x = crack_tip
429:     for vertex in vertices(mesh):
430:         if damage_values[vertex.index()] >= 0.95:
431:             if vertex.point().x() > rightmost_x:
432:                 rightmost_vertex = vertex
433:                 rightmost_x = vertex.point().x()
434: 
435:     crack_length.append(rightmost_x)
436:     print("\ncrack_length:", crack_length)
437: 
438:     # J integral calculation
439:     F_1 = F[0, 0]
440:     F_2 = F[1, 0]
441:     F_1_vector = as_vector([F_1, F_2])
442:     J_expression = -1*(energy_density_function(u, damage)*grad(q_J_integral)[0] \
443:                         - inner(P(u, damage), outer(F_1_vector, grad(q_J_integral))))
444:     J_integral = assemble(J_expression*dx(1))
445:     J_integral_list.append(J_integral)
446:     print("\nJ Integral List:", J_integral_list)
447: 
448:     # Calculating the total force on the top boundary
449:     P_12 = P(u, damage)[0, 1]
450:     P_22 = P(u, damage)[1, 1]
451:     traction_y = assemble(P_22*ds(1))
452:     traction_y_list.append(traction_y)
453:     print("\ntraction y is:", traction_y_list)
454:     
455: 
```

### 分段内逐行解释

#### 第 419 行
- 该行代码：`    # Update the displacement with each iteration`
- 逐行解释：这是注释行，作用是给“边界位移更新与后处理阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 420 行
- 该行代码：`    u1.t = t`
- 逐行解释：边界位移表达式中的时间参数在每个载荷步末更新，为下一个载荷步提供新的加载幅值。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ u1.t = t ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 421 行
- 该行代码：`    u2.t = t`
- 逐行解释：这一行执行赋值，把右侧表达式 `t` 的结果保存到变量 `u2.t` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ u2.t = t ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 422 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“边界位移更新与后处理阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“边界位移更新与后处理阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 423 行
- 该行代码：`    # Post-processing`
- 逐行解释：这是注释行，作用是给“边界位移更新与后处理阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 424 行
- 该行代码：`    # ----------------------------------------`
- 逐行解释：这是注释行，作用是给“边界位移更新与后处理阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 425 行
- 该行代码：`    damage_values = damage.compute_vertex_values(mesh)`
- 逐行解释：从顶点值中读取损伤，接下来会通过“最右侧损伤顶点”估计裂纹长度。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ damage_values = damage.compute_vertex_values(mesh) ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 426 行
- 该行代码：`    # Find the rightmost node with damage >= 0.9`
- 逐行解释：这是注释行，作用是给“边界位移更新与后处理阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 427 行
- 该行代码：`    rightmost_node = None`
- 逐行解释：这一行执行赋值，把右侧表达式 `None` 的结果保存到变量 `rightmost_node` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ rightmost_node = None ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 428 行
- 该行代码：`    rightmost_x = crack_tip`
- 逐行解释：这一行执行赋值，把右侧表达式 `crack_tip` 的结果保存到变量 `rightmost_x` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ rightmost_x = crack_tip ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 429 行
- 该行代码：`    for vertex in vertices(mesh):`
- 逐行解释：这一行开启循环，说明程序要对一组载荷步、顶点或向量分量重复执行同一类操作。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行对应算法迭代或遍历结构，数学上可理解为重复执行同一离散更新。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 430 行
- 该行代码：`        if damage_values[vertex.index()] >= 0.95:`
- 逐行解释：阈值 0.95 用于把“几乎完全损伤”的点视为裂纹面的一部分，这是一个后处理定义。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ d(\mathbf{x}_v) \ge 0.95 ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 431 行
- 该行代码：`            if vertex.point().x() > rightmost_x:`
- 逐行解释：这一行开始一个条件分支，表示程序会根据当前变量状态选择不同路径；这通常意味着模型里存在阈值、边界保护或输出控制。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行体现条件逻辑，而非新的数学算子定义。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 432 行
- 该行代码：`                rightmost_vertex = vertex`
- 逐行解释：这一行执行赋值，把右侧表达式 `vertex` 的结果保存到变量 `rightmost_vertex` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ rightmost_vertex = vertex ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 433 行
- 该行代码：`                rightmost_x = vertex.point().x()`
- 逐行解释：这一行执行赋值，把右侧表达式 `vertex.point().x()` 的结果保存到变量 `rightmost_x` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ rightmost_x = vertex.point().x() ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 434 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“边界位移更新与后处理阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“边界位移更新与后处理阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 435 行
- 该行代码：`    crack_length.append(rightmost_x)`
- 逐行解释：这一行位于“边界位移更新与后处理阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ a_{\mathrm{crack}} = x_{\mathrm{rightmost}} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 436 行
- 该行代码：`    print("\ncrack_length:", crack_length)`
- 逐行解释：这一行把关键中间结果打印到终端，便于监控求解进度、收敛性和后处理数据。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

#### 第 437 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“边界位移更新与后处理阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“边界位移更新与后处理阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 19 个执行点，解释顺序与源码顺序保持一致。

#### 第 438 行
- 该行代码：`    # J integral calculation`
- 逐行解释：这是注释行，作用是给“边界位移更新与后处理阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 20 个执行点，解释顺序与源码顺序保持一致。

#### 第 439 行
- 该行代码：`    F_1 = F[0, 0]`
- 逐行解释：这一行执行赋值，把右侧表达式 `F[0, 0]` 的结果保存到变量 `F_1` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ F_1 = F[0, 0] ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 21 个执行点，解释顺序与源码顺序保持一致。

#### 第 440 行
- 该行代码：`    F_2 = F[1, 0]`
- 逐行解释：这一行执行赋值，把右侧表达式 `F[1, 0]` 的结果保存到变量 `F_2` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ F_2 = F[1, 0] ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 22 个执行点，解释顺序与源码顺序保持一致。

#### 第 441 行
- 该行代码：`    F_1_vector = as_vector([F_1, F_2])`
- 逐行解释：这一行执行赋值，把右侧表达式 `as_vector([F_1, F_2])` 的结果保存到变量 `F_1_vector` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ F_1_vector = as_vector([F_1, F_2]) ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 23 个执行点，解释顺序与源码顺序保持一致。

#### 第 442 行
- 该行代码：`    J_expression = -1*(energy_density_function(u, damage)*grad(q_J_integral)[0] \`
- 逐行解释：J 积分被写成域积分形式，第一项来自能量密度与权函数梯度，第二项来自 Eshelby 型通量项。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ J_{\mathrm{expr}} = -\left(\psi\,\partial_x q - \mathbf{P} : (\mathbf{F}_{\cdot 1}\otimes \nabla q)\right) ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 24 个执行点，解释顺序与源码顺序保持一致。

#### 第 443 行
- 该行代码：`                        - inner(P(u, damage), outer(F_1_vector, grad(q_J_integral))))`
- 逐行解释：这一行位于“边界位移更新与后处理阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 25 个执行点，解释顺序与源码顺序保持一致。

#### 第 444 行
- 该行代码：`    J_integral = assemble(J_expression*dx(1))`
- 逐行解释：这一行执行赋值，把右侧表达式 `assemble(J_expression*dx(1))` 的结果保存到变量 `J_integral` 中；后续主流程会直接引用这个量。
- 运行影响：这一行会把弱式中的积分真正计算成标量，是监测物理量或后处理指标的关键步骤。
- 公式/符号：[ J = \int_{\Omega_J} J_{\mathrm{expr}}\,\mathrm{d}x ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 26 个执行点，解释顺序与源码顺序保持一致。

#### 第 445 行
- 该行代码：`    J_integral_list.append(J_integral)`
- 逐行解释：这一行位于“边界位移更新与后处理阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 27 个执行点，解释顺序与源码顺序保持一致。

#### 第 446 行
- 该行代码：`    print("\nJ Integral List:", J_integral_list)`
- 逐行解释：这一行把关键中间结果打印到终端，便于监控求解进度、收敛性和后处理数据。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 28 个执行点，解释顺序与源码顺序保持一致。

#### 第 447 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“边界位移更新与后处理阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“边界位移更新与后处理阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 29 个执行点，解释顺序与源码顺序保持一致。

#### 第 448 行
- 该行代码：`    # Calculating the total force on the top boundary`
- 逐行解释：这是注释行，作用是给“边界位移更新与后处理阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 30 个执行点，解释顺序与源码顺序保持一致。

#### 第 449 行
- 该行代码：`    P_12 = P(u, damage)[0, 1]`
- 逐行解释：这一行执行赋值，把右侧表达式 `P(u, damage)[0, 1]` 的结果保存到变量 `P_12` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ P_{12} = \mathbf{P}_{12} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 31 个执行点，解释顺序与源码顺序保持一致。

#### 第 450 行
- 该行代码：`    P_22 = P(u, damage)[1, 1]`
- 逐行解释：这一行执行赋值，把右侧表达式 `P(u, damage)[1, 1]` 的结果保存到变量 `P_22` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ P_{22} = \mathbf{P}_{22} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 32 个执行点，解释顺序与源码顺序保持一致。

#### 第 451 行
- 该行代码：`    traction_y = assemble(P_22*ds(1))`
- 逐行解释：通过积分顶边界上的名义应力分量 P22，程序得到竖向总反力，也就是载荷-位移曲线的纵坐标。
- 运行影响：这一行会把弱式中的积分真正计算成标量，是监测物理量或后处理指标的关键步骤。
- 公式/符号：[ T_y = \int_{\Gamma_{\mathrm{top}}} P_{22}\,\mathrm{d}s ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 33 个执行点，解释顺序与源码顺序保持一致。

#### 第 452 行
- 该行代码：`    traction_y_list.append(traction_y)`
- 逐行解释：这一行位于“边界位移更新与后处理阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 34 个执行点，解释顺序与源码顺序保持一致。

#### 第 453 行
- 该行代码：`    print("\ntraction y is:", traction_y_list)`
- 逐行解释：这一行把关键中间结果打印到终端，便于监控求解进度、收敛性和后处理数据。
- 运行影响：这行执行后会在“边界位移更新与后处理阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行无单独提炼的显式公式。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 35 个执行点，解释顺序与源码顺序保持一致。

#### 第 454 行
- 该行代码：`    `
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 36 个执行点，解释顺序与源码顺序保持一致。

#### 第 455 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“当前阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“当前阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“边界位移更新与后处理阶段”内部的第 37 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 420 行对应公式：[ u1.t = t ]
- 第 421 行对应公式：[ u2.t = t ]
- 第 425 行对应公式：[ damage_values = damage.compute_vertex_values(mesh) ]
- 第 427 行对应公式：[ rightmost_node = None ]
- 第 428 行对应公式：[ rightmost_x = crack_tip ]
- 第 430 行对应公式：[ d(\mathbf{x}_v) \ge 0.95 ]
- 第 432 行对应公式：[ rightmost_vertex = vertex ]
- 第 433 行对应公式：[ rightmost_x = vertex.point().x() ]
- 第 435 行对应公式：[ a_{\mathrm{crack}} = x_{\mathrm{rightmost}} ]
- 第 439 行对应公式：[ F_1 = F[0, 0] ]

## 分段 18：第 456-487 行 —— 结果保存与绘图阶段

- 本段定位：把后处理数组保存为文本，再绘制 J-裂纹长度曲线和位移-反力曲线。
- 与前后关系：这是最后一个分段，完成结果收尾。
- 阅读方式：先看下面的源码分段，再看后面的“分段内逐行解释”和“本段公式对应小结”。

### 本段源码（代码在前，附原始行号）

```python
456: # ----------------------------------------------------------------------------
457: crack_length_arr = np.array(crack_length)
458: J_integral_arr = np.array(J_integral_list)
459: traction_y_arr = np.array(traction_y_list)
460: np.savetxt(savedir + f'/J_disp.txt', np.column_stack((crack_length_arr, J_integral_arr)), \
461:             header='Crack Length | J Integral List', fmt='%f', delimiter=' | ')
462: np.savetxt(savedir + f'/traction_disp.txt', np.column_stack((load_multipliers, traction_y_arr)), \
463:             header='displacament | traction', fmt='%f', delimiter=' | ')
464: 
465: 
466: num_plot = load_steps
467: plt.figure(1)
468: plt.plot(crack_length[1:num_plot], J_integral_list[1:num_plot], label='total')
469: plt.xlabel('Crack length')
470: plt.ylabel('J')
471: plt.title('J Integral')
472: plt.legend()
473: plt.savefig(savedir + '/J_Integral.pdf', transparent=True)
474: plt.show()
475: 
476: plt.figure(2)
477: plt.plot(load_multipliers[1:num_plot], traction_y_list[1:num_plot])
478: plt.xlabel('Displacement')
479: plt.ylabel('Total force')
480: plt.title('Traction')
481: plt.savefig(savedir + '/traction_disp.pdf', transparent=True)
482: 
483: 
484: 
485: 
486: 
487: 
```

### 分段内逐行解释

#### 第 456 行
- 该行代码：`# ----------------------------------------------------------------------------`
- 逐行解释：这是注释行，作用是给“结果保存与绘图阶段”提供语义标题或补充说明；程序执行时不会进入计算，但对理解主流程非常重要。
- 运行影响：这行不会改变变量值，不过它告诉读者接下来的一段代码在主流程中的职责是什么。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 1 个执行点，解释顺序与源码顺序保持一致。

#### 第 457 行
- 该行代码：`crack_length_arr = np.array(crack_length)`
- 逐行解释：这一行执行赋值，把右侧表达式 `np.array(crack_length)` 的结果保存到变量 `crack_length_arr` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathbf{a}_{\mathrm{crack}} = \mathrm{array}(\mathrm{crack\_length}) ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 2 个执行点，解释顺序与源码顺序保持一致。

#### 第 458 行
- 该行代码：`J_integral_arr = np.array(J_integral_list)`
- 逐行解释：这一行执行赋值，把右侧表达式 `np.array(J_integral_list)` 的结果保存到变量 `J_integral_arr` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathbf{J} = \mathrm{array}(J_{\mathrm{integral\_list}}) ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 3 个执行点，解释顺序与源码顺序保持一致。

#### 第 459 行
- 该行代码：`traction_y_arr = np.array(traction_y_list)`
- 逐行解释：这一行执行赋值，把右侧表达式 `np.array(traction_y_list)` 的结果保存到变量 `traction_y_arr` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathbf{T}_y = \mathrm{array}(\mathrm{traction\_y\_list}) ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 4 个执行点，解释顺序与源码顺序保持一致。

#### 第 460 行
- 该行代码：`np.savetxt(savedir + f'/J_disp.txt', np.column_stack((crack_length_arr, J_integral_arr)), \`
- 逐行解释：把裂纹长度与 J 积分并排存盘，便于外部工具直接画出断裂驱动力随裂纹扩展的变化。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathrm{save}\left([a_{\mathrm{crack}}, J]\right) ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 5 个执行点，解释顺序与源码顺序保持一致。

#### 第 461 行
- 该行代码：`            header='Crack Length | J Integral List', fmt='%f', delimiter=' | ')`
- 逐行解释：这一行执行赋值，把右侧表达式 `'Crack Length | J Integral List', fmt='%f', delimiter=' | ')` 的结果保存到变量 `header` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ header = 'Crack Length | J Integral List', fmt='%f', delimiter=' | ') ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 6 个执行点，解释顺序与源码顺序保持一致。

#### 第 462 行
- 该行代码：`np.savetxt(savedir + f'/traction_disp.txt', np.column_stack((load_multipliers, traction_y_arr)), \`
- 逐行解释：这一行位于“结果保存与绘图阶段”中，承担承上启下的程序语义：它要么细化前一行的表达，要么为下一步计算准备上下文。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \mathrm{save}\left([t, T_y]\right) ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 7 个执行点，解释顺序与源码顺序保持一致。

#### 第 463 行
- 该行代码：`            header='displacament | traction', fmt='%f', delimiter=' | ')`
- 逐行解释：这一行不是新的赋值语句，而是上一行 `np.savetxt` 调用的续行参数；它为输出文件补充表头、数值格式和分隔符，其中表头沿用了源码中的原始拼写 `displacament | traction`，其语义应理解为 `displacement | traction`。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{表头字符串源码写作 } \texttt{displacament | traction} ,\ \text{其语义可读作 } \texttt{displacement | traction} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 8 个执行点，解释顺序与源码顺序保持一致。

#### 第 464 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“结果保存与绘图阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“结果保存与绘图阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 9 个执行点，解释顺序与源码顺序保持一致。

#### 第 465 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“结果保存与绘图阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“结果保存与绘图阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 10 个执行点，解释顺序与源码顺序保持一致。

#### 第 466 行
- 该行代码：`num_plot = load_steps`
- 逐行解释：这一行执行赋值，把右侧表达式 `load_steps` 的结果保存到变量 `num_plot` 中；后续主流程会直接引用这个量。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ num_plot = load_steps ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 11 个执行点，解释顺序与源码顺序保持一致。

#### 第 467 行
- 该行代码：`plt.figure(1)`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 12 个执行点，解释顺序与源码顺序保持一致。

#### 第 468 行
- 该行代码：`plt.plot(crack_length[1:num_plot], J_integral_list[1:num_plot], label='total')`
- 逐行解释：第一张图画的是裂纹长度与 J 积分关系，跳过索引 0 通常是为了避免初始步的平凡点影响显示。
- 运行影响：这一行不会反馈到求解器，但会把前面已经得到的数据转成可视化结果，方便人工验算。
- 公式/符号：[ \{(a_{\mathrm{crack},i}, J_i)\}_{i=1}^{N_{\mathrm{plot}}-1} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 13 个执行点，解释顺序与源码顺序保持一致。

#### 第 469 行
- 该行代码：`plt.xlabel('Crack length')`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 14 个执行点，解释顺序与源码顺序保持一致。

#### 第 470 行
- 该行代码：`plt.ylabel('J')`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 15 个执行点，解释顺序与源码顺序保持一致。

#### 第 471 行
- 该行代码：`plt.title('J Integral')`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 16 个执行点，解释顺序与源码顺序保持一致。

#### 第 472 行
- 该行代码：`plt.legend()`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 17 个执行点，解释顺序与源码顺序保持一致。

#### 第 473 行
- 该行代码：`plt.savefig(savedir + '/J_Integral.pdf', transparent=True)`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这一行不会反馈到求解器，但会把前面已经得到的数据转成可视化结果，方便人工验算。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 18 个执行点，解释顺序与源码顺序保持一致。

#### 第 474 行
- 该行代码：`plt.show()`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 19 个执行点，解释顺序与源码顺序保持一致。

#### 第 475 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“结果保存与绘图阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“结果保存与绘图阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 20 个执行点，解释顺序与源码顺序保持一致。

#### 第 476 行
- 该行代码：`plt.figure(2)`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 21 个执行点，解释顺序与源码顺序保持一致。

#### 第 477 行
- 该行代码：`plt.plot(load_multipliers[1:num_plot], traction_y_list[1:num_plot])`
- 逐行解释：第二张图画的是加载位移与反力关系，对应实验或数值分析中常见的载荷-位移曲线。
- 运行影响：这一行不会反馈到求解器，但会把前面已经得到的数据转成可视化结果，方便人工验算。
- 公式/符号：[ \{(t_i, T_{y,i})\}_{i=1}^{N_{\mathrm{plot}}-1} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 22 个执行点，解释顺序与源码顺序保持一致。

#### 第 478 行
- 该行代码：`plt.xlabel('Displacement')`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 23 个执行点，解释顺序与源码顺序保持一致。

#### 第 479 行
- 该行代码：`plt.ylabel('Total force')`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 24 个执行点，解释顺序与源码顺序保持一致。

#### 第 480 行
- 该行代码：`plt.title('Traction')`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这行执行后会在“结果保存与绘图阶段”中推进一小步，使当前阶段能够顺利衔接到下一行或下一段。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 25 个执行点，解释顺序与源码顺序保持一致。

#### 第 481 行
- 该行代码：`plt.savefig(savedir + '/traction_disp.pdf', transparent=True)`
- 逐行解释：这一行属于 Matplotlib 绘图流程，用来把已经计算出的数组结果转成图像文件。
- 运行影响：这一行不会反馈到求解器，但会把前面已经得到的数据转成可视化结果，方便人工验算。
- 公式/符号：[ \text{本行负责结果输出，不引入新的物理公式。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 26 个执行点，解释顺序与源码顺序保持一致。

#### 第 482 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“文件结尾留白阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“文件结尾留白阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 27 个执行点，解释顺序与源码顺序保持一致。

#### 第 483 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“文件结尾留白阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“文件结尾留白阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 28 个执行点，解释顺序与源码顺序保持一致。

#### 第 484 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“文件结尾留白阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“文件结尾留白阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 29 个执行点，解释顺序与源码顺序保持一致。

#### 第 485 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“文件结尾留白阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“文件结尾留白阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 30 个执行点，解释顺序与源码顺序保持一致。

#### 第 486 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“文件结尾留白阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“文件结尾留白阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 31 个执行点，解释顺序与源码顺序保持一致。

#### 第 487 行
- 该行代码：``
- 逐行解释：这是一个空行，用来在“文件结尾留白阶段”内部切分逻辑层次；它不改变数值结果，但能帮助读者和解释文档识别阶段边界。
- 运行影响：运行到这里时不会更新任何物理量，但会让“文件结尾留白阶段”的结构在阅读上更清晰。
- 公式/符号：[ \text{本行无独立数学公式，主要承担注释或结构分隔作用。} ]
- 行内顺序关系：这一行属于“结果保存与绘图阶段”内部的第 32 个执行点，解释顺序与源码顺序保持一致。

### 本段公式对应小结

- 第 457 行对应公式：[ \mathbf{a}_{\mathrm{crack}} = \mathrm{array}(\mathrm{crack\_length}) ]
- 第 458 行对应公式：[ \mathbf{J} = \mathrm{array}(J_{\mathrm{integral\_list}}) ]
- 第 459 行对应公式：[ \mathbf{T}_y = \mathrm{array}(\mathrm{traction\_y\_list}) ]
- 第 460 行对应公式：[ \mathrm{save}\left([a_{\mathrm{crack}}, J]\right) ]
- 第 461 行对应公式：[ header = 'Crack Length | J Integral List', fmt='%f', delimiter=' | ') ]
- 第 462 行对应公式：[ \mathrm{save}\left([t, T_y]\right) ]
- 第 463 行对应公式：[ \text{表头字符串源码写作 } \texttt{displacament | traction} ,\ \text{其语义可读作 } \texttt{displacement | traction} ]
- 第 466 行对应公式：[ num_plot = load_steps ]
- 第 468 行对应公式：[ \{(a_{\mathrm{crack},i}, J_i)\}_{i=1}^{N_{\mathrm{plot}}-1} ]
- 第 477 行对应公式：[ \{(t_i, T_{y,i})\}_{i=1}^{N_{\mathrm{plot}}-1} ]

